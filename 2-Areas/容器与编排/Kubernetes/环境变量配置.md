# 环境变量配置

环境变量配置相关技术笔记。

通过 `spec.containers[].env` 为 Pod 中的容器注入环境变量，实现配置与镜像解耦。

```yaml
    spec:
      containers:
        - env:
            - name: hello
              value: nihao
            - name: hello1
              value: nihao1
```
