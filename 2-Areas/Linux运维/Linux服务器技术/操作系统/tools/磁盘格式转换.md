# 磁盘格式转换

虚拟磁盘文件格式转换（如 qcow2 转 vmdk、raw 转 qcow2 等）。

QCOW2格式的虚拟磁盘文件转换为VMDK格式

```bash
sudo apt-get install qemu-utils
```

打开终端，并执行以下命令来进行转换：

```bash
qemu-img convert -f qcow2 -O vmdk [输入文件].qcow2 [输出文件].vmdk
qemu-img convert -f raw .\m-uf68w6yjuskaudt0vnnw_data_xvdb.raw -O vmdk .\data.vmdk
```
