# adb 安卓调试桥安装

ADB（Android Debug Bridge）调试工具安装。

```bash
sudo apt install android-tools-adb
sudo apt install android-tools-fastboot
您可以使用以下命令检查ADB版本：
$ adb version
安装后，ADB服务器将在其上启动，但是如果不是这种情况，则可以使用以下命令启动ADB服务器：
sudo adb start-server
```
