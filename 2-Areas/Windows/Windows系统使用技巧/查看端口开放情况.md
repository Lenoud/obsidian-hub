# Windows 查看端口开放情况

使用 netstat 命令查看 Windows 系统中 TCP 端口的监听状态。

```cmd
netstat -nao -p tcp | findstr "LISTENING" | findstr "80"
```

参数说明：
- `-nao`：显示所有连接和监听端口，包含进程 PID
- `-p tcp`：仅显示 TCP 协议
- `findstr "LISTENING"`：过滤监听状态的端口
- `findstr "80"`：过滤包含 80 的端口
