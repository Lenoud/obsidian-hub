# Word 正则替换

使用 Word 的通配符功能批量替换文本。

## 示例：去除段落前的时间说明

需要删除的部分起始是"如图"，结尾是"示"，使用通配符表达式 `<(如图)*(示)>`。

操作步骤：点击"更多" → 勾选"使用通配符" → 在查找内容中输入表达式 → 替换为空。

![[1696828833198-cac5f467-10d0-44cc-b113-28cbcc575ec1.png]]
