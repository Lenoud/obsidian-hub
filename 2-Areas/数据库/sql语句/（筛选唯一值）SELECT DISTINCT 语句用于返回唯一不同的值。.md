# SQL SELECT DISTINCT 去重查询

使用 `DISTINCT` 关键字过滤查询结果中的重复值，只返回不同的值。

## 语法

```sql
SELECT DISTINCT column1, column2, ...
FROM table_name;
```

## 说明

| 关键字 | 说明 |
| --- | --- |
| `DISTINCT` | 作用于所有指定列，返回唯一的组合值 |
