# 进程命名空间共享

Kubernetes Pod 的 `shareProcessNamespace` 配置，控制同一 Pod 内容器是否共享进程命名空间。

```yaml
spec:
  shareProcessNamespace: true
```

- `true`：容器共享进程命名空间，可以看到彼此的进程并相互影响
- `false`（默认）：容器拥有独立的进程命名空间

开启后容器之间的进程视图会发生变化，可能影响应用程序行为，需谨慎使用。
