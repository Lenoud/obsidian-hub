**【赛程名称】内部测试**

内部测试

## 【任务1】基于Docker Compose编排部署DjangoBlog博客系统[7.0分]
【适用平台】私有云

### 【题目1】容器化Memcached服务[1.0分]
在master节点/root/DjangoBlog目录下编写Dockerfile-memcached文件构建blog-memcached:v1.0镜像，具体要求如下：（需要用到的软件包：Django.tar.gz）

（1）基础镜像：centos:7.9.2009；

（2）完成memcached服务的安装；

（3）声明端口：11211；

（4）设置服务开机自启。

完成后构建镜像，并提交master节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.6分]

docker run -d --name blog-exam-jiance blog-memcached:v1.0 && sleep 5 && docker exec blog-exam-jiance ps -aux

【判分标准】[0.6分]

 memcached

【判分结束】

【检测命令2】[0.4分]

docker exec blog-exam-jiance cat /etc/redhat-release && docker rm -f blog-exam-jiance

【判分标准】[0.4分]

 7.9.2009

【判分结束】

### 【题目2】容器化Mariadb服务[1.0分]
在master节点/root/DjangoBlog目录下编写Dockerfile-mariadb文件构建blog-mysql:v1.0镜像，具体要求如下：（需要用到的软件包：Django.tar.gz）

（1）基础镜像：centos:7.9.2009；

（2）安装MariaDB服务并设置root用户的密码为root；

（3）创建数据库djangoblog并将sqlfile.sql导入该数据库；

（4）声明端口：3306；

（5）设置服务开机自启。

完成后构建镜像，并提交master节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

docker run -d --name mariadb-exam-jiance blog-mysql:v1.0 && sleep 15 && docker exec mariadb-exam-jiance mysql -uroot -proot -e "status;use djangoblog;show tables" && docker rm -f mariadb-exam-jiance

【判分标准】[1.0分]

root@localhost || 10.3.32-MariaDB || accounts_bloguser || django_admin_log || servermanager_commands

【判分结束】

### 【题目3】容器化前端服务[1.0分]
在master节点/root/DjangoBlog目录下编写Dockerfile-nginx文件构建blog-nginx:v1.0镜像，具体要求如下：（需要用到的软件包：Django.tar.gz）

（1）基础镜像：centos:7.9.2009；

（2）安装nginx服务；

（3）使用提供的nginx.conf作为默认的配置文件；

（3）声明端口：80；

（4）设置服务开机自启。

完成后构建镜像，并提交master节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.4分]

cd /root/DjangoBlog/ && docker build -t blog-nginx:v1.0 -f Dockerfile-nginx .

【判分标准】[0.4分]

nginx.conf || Successfully built

【判分结束】

【检测命令2】[0.6分]

docker run -d --name nginx-exam-jiance blog-nginx:v1.0 && docker logs nginx-exam-jiance && docker rm -f nginx-exam-jiance

【判分标准】[0.6分]

djangoblog

【判分结束】

### 【题目4】容器化Blog服务[1.5分]
在master节点/root/DjangoBlog目录下编写Dockerfile-blog文件构建blog-service:v1.0镜像，具体要求如下：（需要用到的软件包：Django.tar.gz）

（1）基础镜像：centos:7.9.2009；

（2）安装Python3.6环境；

（3）使用pip3工具离线安装requirements.txt中的软件包；

（4）安装DjangoBlog服务；

（5）声明端口：8000；

（6）设置DjangoBlog服务开机自启。

完成后构建镜像，并提交master节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.9分]

cd /root/DjangoBlog/ && docker build -t blog-service:v1.0 -f Dockerfile-blog .

【判分标准】[0.9分]

requirements.txt || pip3 install || --no-index

【判分结束】

【检测命令2】[0.6分]

docker run -d --name blog-exam-jiance blog-service:v1.0 && docker logs blog-exam-jiance && docker rm -f blog-exam-jiance

【判分标准】[0.6分]

Starting djangoblog as root

【判分结束】

### 【题目5】编排部署博客系统[2.5分]
在master节点/root/DjangoBlog目录下编写docker-compose.yaml文件，具体要求如下：

（1）容器1名称：blog-memcached；镜像：blog-memcached:v1.0；端口映射：11211:11211；

（2）容器2名称：blog-mysql；镜像：blog-mysql:v1.0；端口映射：3306:3306；

（3）容器3名称：blog-nginx；镜像：blog-nginx:v1.0；端口映射：80:8888；

（4）容器4名称：blog-service；镜像：blog-service:v1.0；端口映射：8000:8000。

完成后编排部署该博客系统，并提交master节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.4分]

docker-compose -f /root/DjangoBlog/docker-compose.yaml config

【判分标准】[0.4分]

links || container_name: blog-service

【判分结束】

【检测命令2】[0.5分]

curl -L http://$(hostname -i):8888

【判分标准】[0.5分]

djangoblog

【判分结束】

【检测命令3】[0.4分]

docker exec blog-service python3 --version

【判分标准】[0.4分]

Python 3.6.5

【判分结束】

【检测命令4】[0.4分]

docker exec blog-service pip3 list

【判分标准】[0.4分]

Django 3.2.10 || mysqlclient 2.1.0

【判分结束】

【检测命令5】[0.4分]

docker exec blog-nginx cat /etc/nginx/nginx.conf

【判分标准】[0.4分]

/code/djangoblog/collectedstatic/

【判分结束】

【检测命令6】[0.4分]

docker exec blog-service printenv

【判分标准】[0.4分]

DJANGO_MYSQL_DATABASE=djangoblog || DJANGO_MYSQL_PASSWORD=root

【判分结束】

## 【任务2】基于Docker Compose编排部署监控系统[7.0分]
【适用平台】私有云

### 【题目1】容器化部署Node-Exporter [1.0分]
在master节点上编写/root/Monitor/Dockerfile- exporter文件构建monitor-exporter:v1.0镜像，具体要求如下：（需要用到的软件包：Monitor.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）使用二进制包node_exporter-0.18.1.linux-amd64.tar.gz安装node-exporter服务；

（3）声明端口：9100；

（4）设置服务开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.3分]

cd /root/Monitor && docker build -t monitor-exporter:v1.0 -f Dockerfile-exporter .

【判分标准】[0.3分]

node_exporter-0.18.1.linux-amd64.tar.gz || 9100 || Successfully tagged monitor-exporter:v1.0

【判分结束】

【检测命令2】[0.7分]

docker run -d --name exporter-exam-jiance monitor-exporter:v1.0 && sleep 5 && docker exec exporter-exam-jiance ps -aux && docker rm -f exporter-exam-jiance

【判分标准】[0.7分]

 node_exporter

【判分结束】

### 【题目2】容器化部署Alertmanager [1.0分]
在master节点上编写/root/Monitor/Dockerfile-alert文件构建monitor-alert:v1.0镜像，具体要求如下：（需要用到的软件包：Monitor.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）使用提供的二进制包alertmanager-0.19.0.linux-amd64.tar.gz安装Alertmanager服务；

（3）声明端口：9093、9094；

（4）设置服务开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.3分]

cd /root/Monitor/ && docker build -t monitor-alert:v1.0 -f Dockerfile-alert .

【判分标准】[0.3分]

alertmanager-0.19.0.linux-amd64.tar.gz || 9093 || Successfully tagged monitor-alert:v1.0

【判分结束】

【检测命令2】[0.7分]

docker run -d --name alert-exam-jiance monitor-alert:v1.0 && sleep 5 && docker exec alert-exam-jiance ps -aux && docker rm -f alert-exam-jiance

【判分标准】[0.7分]

alertmanager

【判分结束】

### 【题目3】容器化部署Grafana[1.0分]
在master节点上编写/root/Monitor/Dockerfile-grafana文件构建monitor-grafana:v1.0镜像，具体要求如下：（需要用到的软件包：Monitor.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）使用提供的二进制包grafana-6.4.1.linux-amd64.tar.gz安装grafana服务；

（3）声明端口：3000；

（4）设置nacos服务开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.3分]

cd /root/Monitor/ && docker build -t monitor-grafana:v1.0 -f Dockerfile-grafana .

【判分标准】[0.3分]

grafana-6.4.1.linux-amd64.tar.gz || 3000 || Successfully tagged monitor-grafana:v1.0

【判分结束】

【检测命令2】[0.7分]

docker run -d --name grafana-exam-jiance monitor-grafana:v1.0 && sleep 5 && docker exec grafana-exam-jiance ps -aux && docker rm -f grafana-exam-jiance

【判分标准】[0.7分]

grafana-server

【判分结束】

### 【题目4】容器化部署Prometheus[1.5分]
在master节点上编写/root/Monitor/Dockerfile-prometheus文件构建monitor-prometheus:v1.0镜像，具体要求如下：（需要用到的软件包：Monitor.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）使用提供的二进制包prometheus-2.13.0.linux-amd64.tar.gz安装promethues服务；

（3）编写prometheus.yml文件，创建3个任务模板：prometheus、node和alertmanager，并将该文件拷贝到/data/prometheus/目录下；

（4）声明端口：9090；

（5）设置服务开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.3分]

cd /root/Monitor/ && docker build -t monitor-prometheus:v1.0 -f Dockerfile-prometheus .

【判分标准】[0.3分]

prometheus-2.13.0.linux-amd64.tar.gz || 9090 || Successfully tagged monitor-prometheus:v1.0

【判分结束】

【检测命令2】[0.7分]

docker run -d --name prometheus-exam-jiance monitor-prometheus:v1.0 && sleep 5 && docker exec prometheus-exam-jiance ps -aux && docker rm -f prometheus-exam-jiance

【判分标准】[0.7分]

prometheus.yml

【判分结束】

【检测命令3】[0.5分]

source /etc/profile && npm -v

【判分标准】[0.5分]

3.10.10

【判分结束】

### 【题目5】编排部署监控系统[2.5分]
在master节点上编写/root/Monitor/docker-compose.yaml文件，具体要求如下：

（1）容器1名称：monitor-node；镜像：monitor-exporter:v1.0；端口映射：9100:9100；

（2）容器2名称：monitor- alertmanager；镜像：monitor-alert:v1.0；端口映射：9093:9093、9094:9094；

（3）容器3名称：monitor-grafana；镜像：monitor-grafana:v1.0；端口映射：3000:3000；

（4）容器4名称：monitor-prometheus；镜像：monitor-prometheus:v1.0；端口映射：9090:9090。

完成后编排部署监控系统，将Prometheus设置为Grafana的数据源，并命名为Prometheus。然后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

docker-compose -f /root/Monitor/docker-compose.yaml ps|grep running

【判分标准】[0.5分]

monitor-alertmanager || 9093->9093 || monitor-grafana || 3000->3000 || monitor-node || 9100->9100 || monitor-prometheus || 9090->9090

【判分结束】

【检测命令2】[0.5分]

curl -L http://$(hostname -i):9090/targets

【判分标准】[0.5分]

alertmanager (1/1 up) || node (1/1 up) || prometheus (1/1 up)

【判分结束】

【检测命令3】[0.5分]

curl -u admin:admin http://$(hostname -i):3000/datasources

【判分标准】[0.5分]

9090

【判分结束】

【检测命令4】[0.5分]

docker exec monitor-prometheus cat /data/prometheus/prometheus.yml

【判分标准】[0.5分]

node-exporter || alertmanager

【判分结束】

【检测命令5】[0.5分]

curl -L http://$(hostname -i):9093

【判分标准】[0.5分]

Alertmanager

【判分结束】

## 【任务3】基于Docker Compose编排部署Pig快速开发框架[7.0分]
【适用平台】私有云

### 【题目1】容器化部署MariaDB[1.0分]
在master节点上编写/root/Pig/Dockerfile-mariadb文件构建pig-mariadb:v1.0镜像，具体要求如下：（需要用到的软件包：Pig.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）安装并初始化MariaDB，密码：root；

（3）将pig_codegen.sql、pig_config.sql、pig_job.sql、pig.sql导入数据库；

（4）声明端口：3306；

（5）设置MariaDB开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.4分]

cd /root/Pig && docker build -t pig-mariadb:v1.0 -f Dockerfile-mariadb .

【判分标准】[0.4分]

EXPOSE 3306 || mysqld_safe || Successfully built || pig-mariadb:v1.0

【判分结束】

【检测命令2】[0.6分]

docker run -d --name mariadb-exam-jiance pig-mariadb:v1.0 && sleep 10 && docker exec mariadb-exam-jiance mysql -uroot -proot -e "show databases;" && docker rm -f mariadb-exam-jiance

【判分标准】[0.6分]

 pig_codegen || pig_config

【判分结束】

### 【题目2】容器化部署Redis[1.0分]
在master节点上编写/root/Pig/Dockerfile-redis文件构建pig-redis:v1.0镜像，具体要求如下：（需要用到的软件包：Pig.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）安装redis服务；

（3）修改配置文件中的bind 127.0.0.1为bind 0.0.0.0；

（4）设置redis免密，并关闭保护模式；

（5）声明端口：6379；

（6）设置服务开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.3分]

cd /root/Pig && docker build -t pig-redis:v1.0 -f Dockerfile-redis .

【判分标准】[0.3分]

6379 || redis-server || Successfully built

【判分结束】

【检测命令2】[0.4分]

docker run -d --name=redis-exam-jiance pig-redis:v1.0 && sleep 10 && docker exec redis-exam-jiance redis-cli -p 6379 info

【判分标准】[0.4分]

connected_clients:1

【判分结束】

【检测命令3】[0.3分]

docker exec redis-exam-jiance egrep -v '^$|^#' /etc/redis.conf && docker rm -f redis-exam-jiance

【判分标准】[0.3分]

bind 0.0.0.0 || protected-mode no

【判分结束】

### 【题目3】容器化部署Pig[1.5分]
在master节点上编写/root/Pig/Dockerfile-pig文件构建pig-server:v1.0镜像，具体要求如下：（需要用到的软件包：Pig.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）安装openjdk服务；

（3）声明端口：3000、4000、8848、9999；

（4）使用提供的jar包启动pig服务并设置开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.2分]

cd /root/Pig/ && docker build -t pig-server:v1.0 -f Dockerfile-pig .

【判分标准】[0.2分]

8848 || Successfully tagged pig-server:v1.0

【判分结束】

【检测命令2】[0.6分]

docker run -d --name=pig-exam-jiance pig-server:v1.0 && sleep 20 && docker exec pig-exam-jiance java -version

【判分标准】[0.6分]

1.8.0_312

【判分结束】

【检测命令3】[0.7分]

docker exec pig-exam-jiance ps -aux && docker rm -f pig-exam-jiance

【判分标准】[0.7分]

pig-register.jar

【判分结束】

### 【题目4】容器化部署前端服务[1.0分]
在master节点上编写/root/Pig/Dockerfile-nginx文件构建pig-ui:v1.0镜像，具体要求如下：（需要用到的软件包：Pig.tar.gz）

（1）基础镜像：centos:centos7.9.2009；

（2）安装nginx服务；

（3）将dist拷贝到/data目录下，并将pig-ui.conf拷贝到/etc/nginx/conf.d/目录下；

（4）设置nginx服务开机自启。

完成后构建镜像，并提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.4分]

cd /root/Pig/ && docker build -t pig-ui:v1.0 -f Dockerfile-nginx .

【判分标准】[0.4分]

daemon off || Successfully tagged pig-ui:v1.0

【判分结束】

【检测命令2】[0.6分]

docker run -d --name=nginx-exam-jiance --restart=always pig-ui:v1.0 && docker cp nginx-exam-jiance:/data /tmp && ls /tmp/data && rm -rf /tmp/data && docker rm -f nginx-exam-jiance

【判分标准】[0.6分]

fonts || index.html

【判分结束】

### 【题目5】编排部署Pig快发开发平台[2.5分]
在master节点上编写/root/Pig/docker-compose.yaml文件，具体要求如下：

（1）容器1名称：pig-mysql；镜像：pig-mariadb:v1.0；端口映射：3306:3306；

（2）容器2名称： pig-redis；镜像：pig-redis:v1.0；端口映射：6379:6379；

（3）容器3名称：pig-service；镜像：pig-server:v1.0；端口映射：8848:8848、9999:9999；

（4）容器4名称：pig-ui；镜像：pig-ui:v1.0；端口映射：80:8888。

完成后编排部署Pig快速开发平台。然后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

docker-compose -f Pig/docker-compose.yaml ps

【判分标准】[0.5分]

pig-service || 8848->8848/tcp || pig-ui || 8888->80/tcp

【判分结束】

【检测命令2】[0.5分]

docker exec pig-service ps -aux

【判分标准】[0.5分]

pig-auth.jar || pig-upms-biz.jar

【判分结束】

【检测命令3】[1.0分]

curl -L http://localhost:8888|grep pig

【判分标准】[1.0分]

pig4cloud.com

【判分结束】

【检测命令4】[0.5分]

docker exec pig-ui ps -aux

【判分标准】[0.5分]

nginx: worker process

【判分结束】

## **【任务4】基于Kubernetes + GitLab CI构建持续集成[8.0分]**
【适用平台】私有云

### **【题目1】安装GitLab环境[1.5分]**
在Kubernetes集群中新建命名空间kube-ops，将GitLab部署到该命名空间下，Deployment和Service名称均为gitlab，以NodePort方式将80端口对外暴露为30880，并设置GitLab服务root用户的密码为admin123456。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径CICD-Runner.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.8分]

kubectl -n kube-ops exec deploy/gitlab -- gitlab-ctl service-list

```powershell
kubectl -n kube-ops exec deploy/gitlab -- gitlab-ctl service-list |grep "gitlab-monitor*\|gitlab-workhorse*\|postgresql*\|redis*"
```

【判分标准】[0.8分]

gitlab-monitor* || gitlab-workhorse* || postgresql* || redis*

【判分结束】

【检测命令2】[0.3分]

kubectl -n kube-ops exec deploy/gitlab -- printenv

【判分标准】[0.3分]

GITLAB_ROOT_PASSWORD=admin123456

【判分结束】

【检测命令3】[0.4分]

curl -L http://$(hostname -i):30880/admin

【判分标准】[0.4分]

new_user_name

【判分结束】

### **【题目2】部署GitLab Runner[2.0分]**
在Kubernetes集群kube-ops命名空间下使用StatefulSet资源对象完成GitLab Runner的部署，StatefulSet名称为gitlab-ci-runner，副本数为2，并完成GitLab Runner在GitLab中的注册。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径CICD-Runner.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl -n kube-ops get cm -o jsonpath={.items[*].data}

【判分标准】[0.5分]

30880

【判分结束】

【检测命令2】[0.5分]

kubectl -n kube-ops exec statefulset.apps/gitlab-ci-runner -- gitlab-runner list

【判分标准】[0.5分]

gitlab-ci-runner-0 || 30880

【判分结束】

【检测命令3】[0.5分]

kubectl -n kube-ops exec statefulset.apps/gitlab-ci-runner -- cat /home/gitlab-runner/.gitlab-runner/config.toml

【判分标准】[0.5分]

30880

【判分结束】

【检测命令4】[0.5分]

kubectl -n kube-ops logs gitlab-ci-runner-0

【判分标准】[0.5分]

Runner registered successfully

【判分结束】

### **【题目3】配置GitLab[1.0分]**
在GitLab中新建公开项目springcloud，然后将Kubernetes集群添加到GitLab中，项目命名空间选择gitlab。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径CICD-Runner.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

kubectl -n kube-ops exec deploy/gitlab -- cat var/log/gitlab/gitlab-rails/production.log

【判分标准】[1.0分]

6443 || BEGIN CERTIFICATE || "namespace"=>"gitlab" || "project_id"=>"springcloud"

【判分结束】

### **【题目4】构建CICD[3.5分]**
将提供的代码推送到GitLab项目springcloud中，编写流水线脚本.gitlab-ci.yml触发自动构建，要求完成构建代码、构建镜像springcloud:master、推送镜像到library项目并发布服务到gitlab命名空间下。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径CICD-Runner.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

curl -L http://$(hostname -i):30800

【判分标准】[1.0分]

 Hello Golang In Gitlab CI || go1.10.3

【判分结束】

【检测命令2】[0.5分]

kubectl -n kube-system get cm coredns -o json

【判分标准】[0.5分]

gitlab || fallthrough

【判分结束】

【检测命令3】[0.5分]

docker pull $(hostname -i)/library/springcloud:master && docker inspect $(hostname -i)/library/springcloud:master

【判分标准】[0.5分]

/bin/app

【判分结束】

【检测命令4】[0.5分]

kubectl -n gitlab get deploy

【判分标准】[0.5分]

gitlab-k8s-demo-dev 2/2 2 2

【判分结束】

【检测命令5】[1.0分]

cd /tmp/ && git clone http://$(hostname -i):30880/root/springcloud.git >/dev/null 2>&1 && cat springcloud/.gitlab-ci.yml && rm -rf springcloud

【判分标准】[1.0分]

yidaoyun/golang:v1.0 || stages || make build || yidaoyun/docker-dind:v1.0

【判分结束】

## 【任务5】基于Kubernetes + Drone构建持续集成[8.0分]
【适用平台】私有云

该公司决定采用GitLab +Drone来构建CICD环境，以缩短新功能开发上线周期，及时满足客户的需求，实现DevOps的部分流程，来减轻部署运维的负担，实现可视化容器生命周期管理、应用发布和版本迭代更新，请完成GitLab + Drone + Kubernetes的CICD环境部署（构建持续集成所需要的所有软件包在软件包CICD-Runner.tar.gz中）。CICD应用系统架构如下：

![[1685589069663-04187628-2fb4-4369-b554-a26c764b77f3.png]]

### 【题目1】部署GitLab[1.5分]
在Kubernetes集群中新建命名空间devops，在该命名空间下使用镜像gitlab/gitlab-ce部署一个名为gitlab的Deployment，设置GitLab的密码为P@ssw0rd，为该Deployment创建Service，命名为gitlab，并使用NodePort方式将容器的80端口暴露为30888。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

kubectl -n devops get deployment,svc|grep gitlab

【判分标准】[1.0分]

deployment.apps/gitlab 1/1 || service/gitlab NodePort

【判分结束】

【检测命令2】[0.5分]

curl -Lk -XGET -u admin:P@ssw0rd http://$(hostname -i):30888/

【判分标准】[0.5分]

 Sign in

【判分结束】

### 【题目2】配置GitLab[1.0分]
在GitLab中创建一个名为drone的GitLab OAuth应用程序（Drone访问端口为30839），创建一个新项目demo-2048并将提供的项目包demo-2048.tar.gz导入到该项目中。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

curl -L -u root:P@ssw0rd http://$(hostname -i):30888/root/demo-2048/-/tree/drone

【判分标准】[1.0分]

README.md

【判分结束】

### 【题目3】部署Drone[1.0分]
在Kubernetes集群devops命名空间下使用镜像drone/drone:1.10.1创建一个名为drone-server的Deployment，为该Deployment创建一个名为drone-server-service的Service，使用NodePort方式将容器的80端口对外暴露为30839。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.8分]

kubectl -n devops get deployment drone-server -o yaml

【判分标准】[0.8分]

DRONE_GITLAB_CLIENT_ID || DRONE_GITLAB_CLIENT_SECRET

【判分结束】

【检测命令2】[0.2分]

kubectl -n devops get svc

【判分标准】[0.2分]

drone-server-service NodePort

【判分结束】

### 【题目4】配置流水线[1.0分]
在Harbor仓库新建公开项目devops，然后在drone分支下编写流水线脚本.drone.yml，第一阶段名称：build code，负责使用maven构建提供的代码，第二阶段名称：build image & push，负责基于提供的Dockerfile构建镜像并推送到devops项目，第三阶段名称：k8s-service，负责滚动更新服务，第四阶段名称：k8s-deploy，负责将服务发布到Kubernetes集群的devops命名空间并对外暴露服务。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

curl -L http://$(hostname -i):30888/root/demo-2048/-/raw/drone/.drone.yml

【判分标准】[1.0分]

kind: pipeline || mvn clean install || plugins/docker || danielgormly/drone-plugin-kube:0.2.0 || drone

【判分结束】

### 【题目5】构建CICD[3.5分]
触发流水线自动构建，完成流水线的构建，构建成功后访问自动发布的服务。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.5分]

kubectl -n devops get all |grep demo-2048

【判分标准】[1.5分]

service/demo-2048 NodePort || deployment.apps/demo-2048 1/1

【判分结束】

【检测命令2】[1.0分]

curl -L http://$(hostname -i):8889/

【判分标准】[1.0分]

 2048

【判分结束】

【检测命令3】[1.0分]

docker pull $(kubectl -n devops get deployment.apps/demo-2048 -o jsonpath={.spec.template.spec.containers[*].image})

【判分标准】[1.0分]

 Digest: sha256

【判分结束】

## 【任务6】基于Kubernetes + Jenkins + GitLab + Harbor构建持续集成[8.0分]
【适用平台】私有云

### 【题目1】安装Jenkins环境[1.5分]
在Kubernetes集群default命名空间下完成Jenkins的部署，使用镜像jenkins/jenkins:latest，Deployment和Service名称均为jenkins，要求以NodePort方式将Jenkins的8080端口对外暴露为30880，并完成离线插件的安装。部署完成后设置Jenkins用户名为jenkins；密码为000000，并在授权策略中配置“任何用户可以做任何事(没有任何限制)”。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径http://<IP>/Jenkins.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.8分]

kubectl exec deploy/jenkins -- ls /var/jenkins_home/plugins

【判分标准】[0.8分]

pipeline-build-step

【判分结束】

【检测命令2】[0.7分]

curl -L [http://$(kubectl get svc jenkins -o jsonpath={.spec.clusterIP}):8080](http://$(kubectl%20get%20svc%20jenkins%20-o%20jsonpath=%7B.spec.clusterIP%7D):8080)

【判分标准】[0.7分]

jenkins-2.339

【判分结束】

### 【题目2】安装GitLab环境[1.5分]
在Kubernetes集群default命名空间下完成GitLab的部署，使用镜像gitlab/gitlab-ce:latest，Deployment和Service名称均为gitlab，要求在Deployment资源清单文件中设置GitLab的root用户密码为admin123，并以NodePort方式将GitLab的80端口对外暴露为30888。部署完成后新建公开项目springcloud，并将springcloud文件夹中的代码上传到该项目。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径http://<IP>/Jenkins.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl exec -i deploy/gitlab -- gitlab --version

【判分标准】[0.5分]

Gitlab Ruby Gem 4.16.1

【判分结束】

【检测命令2】[0.5分]

curl -L [http://$(kubectl get svc gitlab -o jsonpath={.spec.clusterIP})](http://$(kubectl%20get%20svc%20gitlab%20-o%20jsonpath=%7B.spec.clusterIP%7D))

【判分标准】[0.5分]

navbar-gitlab

【判分结束】

【检测命令3】[0.5分]

kubectl get deployment gitlab -o jsonpath={.spec.template.spec.containers[*].env}

【判分标准】[0.5分]

admin123

【判分结束】

### 【题目3】配置Jenkins连接GitLab[1.0分]
配置Jenkins连接GitLab，在GitLab中生成名为jenkins的“Access Tokens”，在Jenkins中配置GitLab并测试其连通性。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径http://<IP>/Jenkins.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

curl -L http://$(hostname -i):30880/credentials/

【判分标准】[1.0分]

GitLab API token

【判分结束】

### 【题目4】配置Jenkins连接maven[1.0分]
在Jenkins容器中完成Maven的安装，并在Jenkins全局工具配置中新增Maven。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径http://<IP>/Jenkins.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl exec -i deploy/jenkins -- bash -c "source /etc/profile && mvn -v"

【判分标准】[0.5分]

 Apache Maven 3.6.3

【判分结束】

【检测命令2】[0.5分]

curl -L http://$(hostname -i):30880/configureTools/

【判分标准】[0.5分]

value="/usr/local/maven"

【判分结束】

### 【题目5】构建CI/CD[3.0分]
在Jenkins中新建一个流水线任务springcloud，基于GitLab中的springcloud项目，编写流水线脚本，触发构建，完成项目代码构建，并构建docker镜像推送到Harbor仓库的springcloud项目，基于新构建的镜像完成config和gateway服务自动发布到Kubernetes集群springcloud命名空间下。

完成后提交master节点的用户名、密码和IP地址到答题框。（需要用到的软件包路径http://<IP>/Jenkins.tar.gz）

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl exec -i deploy/jenkins -- bash -c "docker images|grep springcloud"

【判分标准】[0.5分]

config || gateway

【判分结束】

【检测命令2】[0.5分]

kubectl exec -i deploy/jenkins -- bash -c 'grep -r "Finished: SUCCESS" /var/jenkins_home/jobs/springcloud/builds'

【判分标准】[0.5分]

Finished: SUCCESS

【判分结束】

【检测命令3】[0.5分]

docker inspect $(docker images|grep config|awk 'NR==1{print$3}')

【判分标准】[0.5分]

"java" || "-Xmx200m" || "-jar" || "/app/config.jar"

【判分结束】

【检测命令4】[0.5分]

curl http://master:30010/

【判分标准】[0.5分]

Accumulation account

【判分结束】

【检测命令5】[1.0分]

kubectl exec -i deploy/jenkins -- bash -c 'cat /var/jenkins_home/jobs/springcloud/config.xml'

【判分标准】[1.0分]

git credentialsId || mvn package -DskipTests || docker build || stage || gateway-deployment.yaml

【判分结束】

## 【任务7】容器云运维--进阶[11.5分]
【适用平台】私有云

### 【题目1】参数调优--修改Pod数量限制[1.0分]
Kubernetes 默认每个节点只能启动110个Pod，由于业务需要，需要在所有节点上运行大量资源消耗非常小的Pod，请将每个节点默认限制的Pod数量改为200。

完成后提交master节点的用户名、密码和IP到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

cat /etc/sysconfig/kubelet

【判分标准】[1.0分]

--max-pods=200

【判分结束】

### 【题目2】参数调优--修改NodePort端口范围[1.0分]
Kubernetes以NodePort方式暴露服务默认的端口范围为30000-32767，请将NodePort的端口范围修改为1024-65535。完成后提交master节点的用户名、密码和IP到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

kubectl describe pod $apiserver_pods -n kube-system

【判分标准】[1.0分]

--service-node-port-range=1024-65535

【判分结束】

### 【题目3】RBAC管理--创建角色并并绑定[1.0分]
创建一个名为deployment-clusterrole的clusterrole，并且对该clusterrole只绑定对Deployment、Daemonset、Statefulset的创建权限。在指定命名空间exam创建一个名为exam-user的serviceaccount，并且将上一步创建clusterrole和该serviceaccount绑定。

完成后提交master节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.4分]

kubectl describe clusterrole deployment-clusterrole

【判分标准】[0.4分]

daemonsets [] [] [create] || statefulsets [] [] [create]

【判分结束】

【检测命令2】[0.6分]

kubectl -n exam describe rolebinding $(kubectl get rolebinding -A|grep exam|awk '{print$2}')

【判分标准】[0.6分]

Name: deployment-clusterrole || ServiceAccount exam-user exam

【判分结束】

### 【题目4】环境变量管理--设置Pod环境变量[1.0分]
在default命名空间下使用nginx:latest镜像启动一个名为env-demo的Pod，为该Pod的容器配置环境变量DEMO_GREETING，其值为“Hello from the environment”。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

kubectl exec env-demo -- printenv

【判分标准】[1.0分]

DEMO_GREETING=Hello from the environment

【判分结束】

### 【题目5】服务质量管理--创建Guaranteed类型的Pood[0.5分]
在default命名空间下使用nginx:latest镜像创建一个QoS类为Guaranteed的Pod，名称为qos-demo。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl get pod qos-demo -o yaml

【判分标准】[0.5分]

qosClass: Guaranteed

【判分结束】

### 【题目6】服务质量管理--创建Burstable类型的Pod[0.5分]
在default命名空间下使用nginx:latest镜像创建一个QoS类为Burstable的Pod，名称为qos-demo。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl get pod qos-demo -o yaml

【判分标准】[0.5分]

qosClass: Burstable

【判分结束】

### 【题目7】生命周期管理--配置Pod生命周期[1.0分]
在default命名空间下使用nginx:latest镜像创建一个名为lifecycle-demo的Pod，要求容器创建成功后执行命令“echo Hello from the postStart handler > /usr/share/message”，容器终止前执行命令“nginx -s quit; while killall -0 nginx; do sleep 1; done”。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl exec -i lifecycle-demo -- cat /usr/share/message

【判分标准】[0.5分]

Hello from the postStart handler

【判分结束】

【检测命令2】[0.5分]

kubectl get pods lifecycle-demo -o yaml

【判分标准】[0.5分]

preStop || while killall -0 nginx

【判分结束】

### 【题目8】存储卷管理--Pod时间同步[1.0分]
容器默认的时区采用的是UTC时区，而宿主机采用的是CST时区。使用nginx:latest镜像在default命名空间下创建一个名为exam的Pod，要求Pod时区与宿主机时区同步。

完成后提交master节点的IP、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

kubectl get pods exam -o yaml

【判分标准】[1.0分]

path: /etc/localtime

【判分结束】

### 【题目9】网络策略管理--创建Pod网络策略[1.0分]
创建一个网络策略network-exam，要求只有internal命名空间下的Pod可以通过TCP协议的8080端口访问到mysql命名空间下的Pod。

完成后提交master节点的IP、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl get ns internal --show-labels

【判分标准】[0.5分]

ns=internal

【判分结束】

【检测命令2】[0.5分]

kubectl -n mysql get networkpolicy -o yaml

【判分标准】[0.5分]

ns: internal || port: 8080

【判分结束】

### 【题目10】服务部署--部署OwnCloud服务[1.5分]
在Kubernetes集群default命名空间下完成OwnCloud云盘系统的部署。启动一个名为owncloud的Deployment，包含一个Pod，Pod内包含两个容器owncloud（镜像：owncloud:latest）和mysql（mysql:5.6）。为该Deployment创建一个名为owncloud-svc的Service，以NodePort方式将容器的80端口对外暴露为30003。

完成后提交master节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

curl -L http://$(hostname -i):30003

【判分标准】[0.5分]

ownCloud

【判分结束】

【检测命令2】[0.5分]

kubectl get deployment owncloud -o jsonpath={.spec.template.spec.containers[*].name}

【判分标准】[0.5分]

owncloud || mysql

【判分结束】

【检测命令3】[0.5分]

kubectl get all

【判分标准】[0.5分]

deployment.apps/owncloud 1/1 1 1 || service/owncloud-svc NodePort

【判分结束】

### 【题目11】服务部署--部署WordPress服务[1.5分]
基于Kubernetes编排部署WordPress，具体要求如下：

（1）在master节点部署NFS服务，做共享存储；

（2）镜像：mysql:5.6、wordpress:latest；

（3）创建PVC mysql-pv-claim，绑定PV mysql-persistent-storage，PV大小：20Gi，权限：RWO，回收策略：Recycle；

（4）创建PVC wp-pv-claim，绑定PV wordpress-persistent-storage，PV大小：20Gi，权限：RWO，回收策略：Recycle；

（5）wordpress服务以NodePort方式对外暴露端口31000。

完成后使用该YAML文件部署WordPress，并提交master节点的用户名、密码和IP到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.3分]

kubectl get pv

【判分标准】[0.3分]

mysql-persistent-storage 20Gi RWO Recycle Bound || wordpress-persistent-storage 20Gi RWO Recycle Bound

【判分结束】

【检测命令2】[0.3分]

kubectl get pvc

【判分标准】[0.3分]

mysql-pv-claim Bound wordpress-persistent-storage 20Gi RWO nfs || wp-pv-claim Bound mysql-persistent-storage 20Gi RWO nfs

【判分结束】

【检测命令3】[0.4分]

kubectl get service

【判分标准】[0.4分]

wordpress NodePort || 80:31000/TCP

【判分结束】

【检测命令4】[0.5分]

curl -L http://master:31000

【判分标准】[0.5分]

[https://wordpress.org](https://wordpress.org) || value="zh_TW" || value="zh_CN" || value="zh_HK" ||

【判分结束】

### 【题目12】安全上下文--为Pod配置安全上下文[0.5分]
使用busybox镜像启动一个名为context-demo的Pod，为该Pod配置安全上下文，要求容器内以用户1000和用户组3000来运行所有进程，并在启动时执行“sleep 1h”命令。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl exec -i context-demo -- id

【判分标准】[0.5分]

uid=1000 || gid=3000

【判分结束】

## 【任务8】Istio服务网格[6.0分]
【适用平台】私有云

### 【题目1】Sidecar管理--自动注入[1.0分]
新建命名空间exam，为该命名空间开启自动注入Sidecar。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

kubectl get ns exam --show-labels

【判分标准】[1.0分]

istio-injection=enabled

【判分结束】

![[1691582702356-d96177c9-ffa8-41a0-8a8b-c5697e221225.jpeg]]

### 【题目2】Sidecar管理--手动注入[1.0分]
在default命名空间下使用busybox镜像创建一个名为busybox的Pod，并手动为该Pod注入Sidecar。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

kubectl get pods busybox --show-labels

【判分标准】[1.0分]

security.istio.io

【判分结束】

### 【题目3】流量管理--创建Ingress Gateway[0.5分]
使用提供的软件包ServiceMesh.tar.gz将Bookinfo应用部署到default命名空间下，使用Istio Gateway可以实现应用程序从外部访问，请为Bookinfo应用创建一个名为bookinfo-gateway的网关，指定所有HTTP流量通过80端口流入网格，然后将网关绑定到虚拟服务bookinfo上。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

curl -L [http://$(hostname -i):$(kubectl -n istio-system get service istio-ingressgateway -o jsonpath='{.spec.ports[?(@.name=="http2")].nodePort}')/productpage](http://$(hostname%20-i):$(kubectl%20-n%20istio-system%20get%20service%20istio-ingressgateway%20-o%20jsonpath='%7B.spec.ports%5B?(@.name==%22http2%22)%5D.nodePort%7D')/productpage)

【判分标准】[0.5分]

Microservices Fabric BookInfo Demo

【判分结束】

### 【题目4】流量管理--创建默认目标规则[0.5分]
为Bookinfo应用的四个微服务设置默认目标规则，名称分别为productpage、reviews、ratings和details。定义好可用的版本，productpage服务可用的版本为v1，reviews服务可用的版本为v1、v2、v3，ratings服务可用的版本为v1、v2，details服务可用的版本为v1、v2。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl get destinationrule reviews -o jsonpath={.spec.subsets}

【判分标准】[0.5分]

"name":"v1" || "name":"v3"

【判分结束】

### 【题目5】流量管理--创建默认请求路由[1.0分]
为Bookinfo应用的四个微服务设置默认版本的VirtualService，名称分别为productpage、reviews、ratings和details，将所有流量路由到每个微服务的v1版本。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.4分]

kubectl get virtualservice

【判分标准】[0.4分]

details ["details"] || productpage ["productpage"] || ratings ["ratings"] || reviews ["reviews"]

【判分结束】

【检测命令2】[0.6分]

kubectl get virtualservice details -o jsonpath={.spec.http}

【判分标准】[0.6分]

"subset":"v1"

【判分结束】

### 【题目6】流量管理--流量镜像[1.0分]
更改请求路由reviews，将100%的流量路由到reviews微服务的v1版本，然后将100%的相同流量镜像到reviews微服务的v2版本。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl get virtualservice reviews -o jsonpath={.spec.http[*].mirror}

【判分标准】[0.5分]

"subset":"v2"

【判分结束】

【检测命令2】[0.5分]

kubectl get virtualservice reviews -o jsonpath={.spec.http[*].route}

【判分标准】[0.5分]

"subset":"v1" || "weight":100

【判分结束】

### 【题目7】流量管理--创建请求路由[1.0分]
在default命名空间下创建一个名为reviews-route的虚拟服务，默认情况下，所有的HTTP流量都会被路由到标签为version:v1的reviews服务的Pod上。此外，路径以/wpcatalog/或/consumercatalog/开头的HTTP请求将被重写为/newcatalog，并被发送到标签为version:v2的Pod上。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl get virtualservice reviews-route -o jsonpath={.spec.http[*].match}

【判分标准】[0.5分]

"uri":{"prefix":"/wpcatalog"} || "uri":{"prefix":"/consumercatalog"}

【判分结束】

【检测命令2】[0.5分]

kubectl get virtualservice reviews-route -o jsonpath={.spec.http[*].rewrite}

【判分标准】[0.5分]

{"uri":"/newcatalog"}

【判分结束】

## 【任务9】KubeVirt运维[1.0分]
【适用平台】私有云

### 【题目1】VM管理--创建VM[1.0分]
使用镜像fedora-virt:v1.0在default命名空间下创建一台vm，名称为vm-fedora，内存为1G。

完成后提交master节点的IP地址、用户名和密码到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[0.5分]

kubectl get vm

【判分标准】[0.5分]

vm-fedora

【判分结束】

【检测命令2】[0.5分]

kubectl get vm -o json

【判分标准】[0.5分]

"memory": "1Gi"

【判分结束】

## 【任务10】Kubernetes运维开发任务[6.0分]
【适用平台】私有云

### 【题目1】Kubernetes Python运维脚本开发：使用SDK方式管理deployment服务 [3.0分]
在提供的OpenStack私有云平台上，使用k8s-python-dev 镜像创建1台云主机，云主机类型使用4vCPU/12G内存/100G硬盘。该主机中已经默认安装了所需的开发环境，登录默认账号密码为“root/1DaoYun@2022”。使用Kubernetes python SDK的“kubernetes”Python库，在/root目录下，创建sdk_manager_deployment.py文件，要求编写python代码，代码实现以下任务：

（1）首先使用nginx-deployment.yaml文件创建deployment资源。

（2）创建完成后，查询该服务的信息，查询的body部分通过控制台输出，并以json格式的文件输出到当前目录下的deployment_sdk_dev.json文件中。

编写完成后，提交该云主机的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

cat sdk_manager_deployment.py

【判分标准】[1.0分]

kubernetes || with open || yaml. || create_namespaced_deployment

【判分结束】

【检测命令2】[1.0分]

python3 sdk_manager_deployment.py

【判分标准】[1.0分]

{'api_version' || 'kind' || 'metadata': || 'host_ip'

【判分结束】

【检测命令3】[1.0分]

cat deployment_sdk_dev.json

【判分标准】[1.0分]

api_version || kind || metadata || host_ip

【判分结束】

### 【题目2】Kubernetes Python运维脚本开发：使用Restful API方式管理service服务 [3.0分]
在提供的OpenStack私有云平台上，使用k8s-python-dev 镜像创建1台云主机，云主机类型使用4vCPU/12G内存/100G硬盘。该主机中已经默认安装了所需的开发环境，登录默认账号密码为“root/1DaoYun@2022”。使用python request库和Kubernetes Restful APIs，在/root目录下，创建api_manager_service.py文件，要求编写python代码，代码实现以下任务：

（1）首先查询查询服务service，如果service名称“nginx-svc”已经存在，先删除。

（2）如果不存在“nginx-svc”，则使用service.yaml文件创建服务。

（3）创建完成后，查询该服务的信息，查询的body部分以json格式的文件输出到当前目录下的service_api_dev.json文件中。

（4）然后使用service_update.yaml更新服务端口。

（5）完成更新后，查询该服务的信息，信息通过控制台输出，并通过json格式追加到service_api_dev.json文件后。

编写完成后，提交该云主机的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【登录管理器】账户密码

【检测命令1】[1.0分]

cat api_manager_service.py

【判分标准】[1.0分]

import requests || yaml || json || bearer

【判分结束】

【检测命令2】[1.0分]

python3 api_manager_service.py

【判分标准】[1.0分]

name || resourceVersion || targetPort || port

【判分结束】

【检测命令3】[1.0分]

cat service_api_dev.json

【判分标准】[1.0分]

name || resourceVersion || targetPort || port

【判分结束】
