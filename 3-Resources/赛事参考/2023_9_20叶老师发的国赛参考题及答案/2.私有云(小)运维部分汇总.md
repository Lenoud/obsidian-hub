**私有云（小）运维汇总**

**【题目1】网络管理**

使用自己搭建的openstack云平台，创建云主机网络extnet，子网extsubnet，虚拟机网段为192.168.100.0/24， 网关为192.168.100.1，段ID默认写100。

1.创建网络extnet

[root@controller ~]# openstack network create --project admin --share --provider-network-type vlan --provider-physical-network provider --external --provider-segment 100 extnet

2.创建子网extsubnet

[root@controller ~]# openstack subnet create –-project admin --network extnet --subnet-range 192.168.100.0/24 --gateway 192.168.100.1 extsubnet

3.查看网络是否创建成功

[root@controller ~]# openstack network list

+--------------------------------------+--------+--------------------------------------+

| ID                                   | Name   | Subnets                              |

+--------------------------------------+--------+--------------------------------------+

| 9a716f26-d8b5-48b4-8e9f-9d90fdb1f76e | extnet | 73b8e6a3-ad88-415b-a7c2-5eb14d6e6df0 |

+--------------------------------------+--------+--------------------------------------+

**【题目2】快照管理**

使用自己搭建的openstack云平台，将云主机VM1保存为qcow2格式的快照并保存到controller节点/root/cloudsave目录下，保存名字为csccvm.qcow2。

1.给云主机VM1创建快照

[root@controller ~]# mkdir /root/cloudsave

[root@controller ~]# nova image-create VM1 csccvm.qcow2

2.查看镜像列表获得镜像ID

[root@controller ~]# glance image-list

+--------------------------------------+--------------+

| ID                                   | Name         |

+--------------------------------------+--------------+

| f4867931-b1ec-4ec3-860e-d7b5213bee20 | cirros       |

| 8fec672c-5610-4b83-a136-7a45929c31f2 | csccvm.qcow2 |

+--------------------------------------+--------------+

3.下载快照到本地

[root@controller ~]# glance image-download --file /root/cloudsave/csccvm.qcow2 8fec672c-5610-4b83-a136-7a45929c31f2

Download(下载)

4.查看

[root@controller ~]# ls cloudsave/

csccvm.qcow2

**【题目3】虚拟机调整flavor**

使用自己搭建的openstack云平台，使用cirros镜像，flavor使用1vcpu/512M内存/10G硬盘，创建云主机cscc_vm，假设在使用过程中，发现该云主机配置太低，需要调整，请修改相应配置，将dashboard界面上的云主机调整实例大小可以使用，将该云主机实例大小调整为1vcpu/1G内存/20G硬盘。

[root@controller ~]# cat /etc/nova/nova.conf

allow_resize_to_same_host=True （取消注释并把False改为True）

[root@controller ~]# systemctl restart openstack-nova-*

后面的调整在dashboard界面上完成即可

**【题目4】swift分片存储**

使用自己搭建的openstack云平台，自行安装Swift服务，新建名为chinaskill的容器，将cirros-0.3.4-x86_64-disk.img镜像上传到chinaskill容器中，并设置分段存放，每一段大小为10M。

[root@controller ~]# swift post chinaskill(创建容器 )

[root@controller ~]# swift stat chinaskill(查看容器状态)

[root@controller ~]# swift upload chinaskill -S 10485760 cirros-0.3.4-x86_64-disk.img(上传镜像并以 **10MB** 大小坡分片存储)

[root@controller ~]# swift stat chinaskill cirros-0.3.4-x86_64-disk.img(查看 **cirros** 镜像的存储路径)

**【题目5】云平台安全策略提升**

使用OpenStack私有云平台，使用http://<IP>/https.tar.gz中的软件包，安装必要组件，将私有云平台的访问策略从http提升至https。

在controller节点上做

1.安装软件包

[root@controller ~]# yum install -y mod_wsgi httpd mod_ssl

2.修改/etc/httpd/conf.d/ssl.conf文件

[root@controller ~]# cat /etc/httpd/conf.d/ssl.conf

#SSLProtocol all -SSLv2 -SSLv3    //找到该行，并注释

SSLProtocol all -SSLv2            //添加该行

2.修改/etc/openstack-dashboard/local_settings文件

[root@controller ~]# cat /etc/openstack-dashboard/local_settings

…

CSRF_COOKIE_SECURE = True     取消注释

SESSION_COOKIE_SECURE = True   取消注释

USE_SSL = True   添加该行

SESSION_COOKIE_HTTPONLY = True   添加该行

…

3.重启httpd服务和memcached服务

[root@controller ~]# systemctl restart httpd memcached

4.浏览器输入https://YOUR IP ADDRESS访问云平台

![[1695216937491-9b3f7136-1c0d-4a5d-b6ec-482b7b4d5b86.png]]

**【题目6】keystone权限控制**

使用自行搭建的OpenStack私有云平台，修改普通用户权限，使普通用户不能对镜像进行创建和删除操作，配置完成后提交控制节点的用户名、密码和IP地址到答题框。

修改controller节点的配置文件(修改完文件后自动生效)

[root@controller ~]# cat /etc/glance/policy.json   修改如下标红的两行

    "add_image": "role:admin",

    "delete_image": "role:admin",

**【题目7】OpenStack 参数调优[1 分]**

OpenStack 各服务内部通信都是通过 RPC 来交互，各 agent 都需要去连接 RabbitMQ； 随着各服务 agent 增多，MQ 的连接数会随之增多，最终可能会到达上限，成为瓶颈。在自行搭建的 OpenStack 私有云平台或赛项提供的 all-in-one 平台上，分别通过用户级别、系统级别、配置文件来设置 RabbitMQ 服务的最大连接数为 10240，配置完成后提交修改节点的用户名、密码和 IP 地址到答题框。

1. 在/etc/security/limits.conf末尾添加如下两行

[root@controller ~]# cat /etc/security/limits.conf

…

openstack       soft    nofile  10240

openstack       hard    nofile  10240

2.在/etc/sysctl.conf文件末尾添加如下一行：

[root@controller ~]# cat /etc/sysctl.conf

…

fs.file-max=10240

3.在/usr/lib/systemd/system/rabbitmq-server.service文件service块中添加如下一行：

[root@controller ~]# cat /usr/lib/systemd/system/rabbitmq-server.service

…

LimitNOFILE=10240

4.重启服务

[root@controller ~]# systemctl daemon-reload

[root@controller ~]# systemctl restart rabbitmq-server

5.查看rabbitmq的状态

[root@controller ~]# rabbitmqctl status | grep total_limit

[{total_limit,10140},

**【题目8】KVM 调优[1 分]**

在自行搭建的 OpenStack 私有云平台或赛项提供的 all-in-one 平台上，修改相关配置文件，启用-device virtio-net-pci in kvm，配置完成后提交控制节点的用户名、密码和 IP 地址到答题框。

1.查看/etc/nova/nova.conf 配置文件，有 libvirt_use_virtio_for_bridges=true 计 1 分

1.在controller节点的/etc/nova/nova.conf文件中添加如下内容：(可以在libvirt块的附近添加,在文件的6333行左右的libvirt模块添加)

[root@controller ~]# cat /etc/nova/nova.conf

--libvirt_use_virtio_for_bridges=true

**【题目9】平台内存优化[1.0分]**

搭建完OpenStack平台后，关闭系统的内存共享，打开透明大页。完成后提交控制节点的用户名、密码（默认Abc@1234）和IP地址到答题框。

[root@controller ~]# echo never > /sys/kernel/mm/transparent_hugepage/defrag

[root@controller ~]# cat /sys/kernel/mm/transparent_hugepage/defrag

always madvise [never]

**【题目10】Nova超时时间[1.0分]**

在OpenStack平台中，由于Python的单进程不能真正的并发，所以RPC请求可能不能及时响应，尤其是目标节点在执行耗时较长的定时任务时，所以需要综合考虑超时时间和等待容忍时间。修改Nova的相关配置文件，将超时时间延长至300。配置完成后提交改动节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/nova/nova.conf

rpc_response_timeout=300  #去掉注释并把60改为300

[root@controller ~]# systemctl restart openstack-nova-*

**【题目11】Redis服务调优-内存大页[1.0分]**

使用提供的OpenStack私有云平台，申请一台centos7.9系统的云主机，使用提供的http源，自行安装Redis服务并启动。因为Redis服务采用了内存大页，生成RDB期间，即使客户端修改的数据只有50B的数据，Redis需要复制2MB的大页。当写的指令比较多的时候就会导致大量的拷贝，导致性能变慢。请修改Redis的内存大页机制，规避大量拷贝时的性能变慢问题。配置完成后提交Redis节点的用户名、密码和IP地址到答题框。

[root@controller ~]# yum -y install redis

[root@controller ~]# systemctl start redis && systemctl enable redis

[root@controller ~]# echo never > /sys/kernel/mm/transparent_hugepage/enabled

[root@controller ~]# cat /sys/kernel/mm/transparent_hugepage/enabled

always madvise [never]

**【题目12】开放镜像[1.0分]**

使用自己搭建的OpenStack私有云平台，在OpenStack平台的admin项目中使用cirros-0.3.4-x86_64-disk.img镜像文件创建名为glance-cirros的镜像，通过OpenStack命令将glance-cirros镜像指定demo项目进行共享使用。配置完成后提交controller点的用户名、密码（默认Abc@1234）和IP地址到答题框。

[root@controller ~]# source /etc/keystone/admin-openrc.sh

[root@controller ~]# glance image-create --name cirros --disk-format qcow2 --container-format bare --progress < cirros-0.3.4-x86_64-disk.img

[root@controller ~]# glance image-list

+--------------------------------------+-----------+

| ID                                   | Name      |

+--------------------------------------+-----------+

| e76d7081-b3d9-4a36-b3f3-23da46b8090d | cirros    |

+--------------------------------------+-----------+

[root@controller ~]# openstack project list

+----------------------------------+------------------------------------------------------------------+

| ID                               | Name                                                             |

+----------------------------------+------------------------------------------------------------------+

| 7dd79b3360384746a894360dff1bfc00 | demo                                                             |

| 815410af791d413abc003f3f0a8955aa | admin                                                            |

| ac33947817ae4b658954e040a3084f89 | service                                                          |

| c263a400a9d5426585cfae13b80a6bb8 | 815410af791d413abc003f3f0a8955aa-6cabe70e-783c-4b36-a054-5de32e7 |

+----------------------------------+------------------------------------------------------------------+

**把demo加到镜像member中**

[root@controller ~]# glance member-create e76d7081-b3d9-4a36-b3f3-23da46b8090d dd79b3360384746a894360dff1bfc00

+--------------------------------------+---------------------------------+---------+

| Image ID                             | Member ID                       | Status  |

+--------------------------------------+---------------------------------+---------+

| e76d7081-b3d9-4a36-b3f3-23da46b8090d | dd79b3360384746a894360dff1bfc00 | pending |

+--------------------------------------+---------------------------------+---------+

**admin这边把demo加入到member中，还需要demo这边确认，把member状态更新为accepted，表示接收共享镜像**

[root@controller ~]# glance member-update e76d7081-b3d9-4a36-b3f3-23da46b8090d dd79b3360384746a894360dff1bfc00 accepted

+--------------------------------------+---------------------------------+----------+

| Image ID                             | Member ID                       | Status   |

+--------------------------------------+---------------------------------+----------+

| e76d7081-b3d9-4a36-b3f3-23da46b8090d | dd79b3360384746a894360dff1bfc00 | accepted |

+--------------------------------------+---------------------------------+----------+

[root@controller ~]# glance member-list --image-id 834dc91e-407b-4706-83f1-1106808b17f9

+--------------------------------------+----------------------------------+----------+

| Image ID                             | Member ID                        | Status   |

+--------------------------------------+----------------------------------+----------+

| 834dc91e-407b-4706-83f1-1106808b17f9 | 14d07bbbb50a47cd8504db1da99f2560 | accepted |

+--------------------------------------+----------------------------------+----------+

**【题目13】Glance运维：调整响应时间[1.0分]**

在Glance服务的配置文件中，有一个glance响应最大返回项数，该参数默认设置过短，可能导致响应数据被截断，修改该参数为1000。完成后提交控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/glance/glance-api.conf

api_limit_max = 1000  找到这行修改为1000

[root@controller ~]# systemctl restart openstack-glance-api

**【题目14】Glance镜像压缩[1.0分]**

使用自行搭建的OpenStack平台。在HTTP服务中存在一个镜像为CentOS7.5-compress.qcow2的镜像，请使用qemu相关命令，对该镜像进行压缩，压缩后的镜像命名为chinaskill-js-compress.qcow2并存放在/root目录下。完成后提交controller点的用户名、密码（默认Abc@1234）和IP地址到答题框。

先自行上传CentOS7.5-compress.qcow2到root目录下

[root@controller ~]# qemu-img convert -p -c -O qcow2 CentOS7.5-compress.qcow2 chinaskill-js-compress.qcow2

    (100.00/100%)

[root@controller ~]# du -sh chinaskill-js-compress.qcow2

405M    chinaskill-js-compress.qcow2

**【题目15】Cinder运维：Cinder作为Glance后端存储[1.0分]**

在自行搭建的OpenStack平台中修改相关参数，使glance可以使用cinder作为后端存储，将镜像存储于cinder卷中。使用cirros-0.3.4-x86_64-disk.img文件创建cirros-image镜像存储于cirros-cinder卷中，通过cirros-image镜像使用cinder卷启动盘的方式进行创建虚拟机。完成后提交修改节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/glance/glance-api.conf

show_multiple_locations = true  #去掉注释并把false改为true

[root@controller ~]# systemctl restart openstack-glance-api

[root@controller ~]# cat /etc/cinder/cinder.conf

allowed_direct_url_schemes = cinder  #去掉注释并写上cinder

image_upload_use_internal_tenant = true  #去掉注释并把false改为true

[root@controller ~]# systemctl restart openstack-cinder-*

这里可以取巧得分，只要openstack image show cirros命令检测出cinder即可，所以可以给镜像cirros-images打个tags名为cinder

[root@controller ~]# glance image-tag-update d31196b3-d7e0-448c-811a-579228f9f78b cinder

+--------------------------------------+--------------------------+

| ID                                   | Tags                     |

+--------------------------------------+--------------------------+

| d31196b3-d7e0-448c-811a-579228f9f78b | ['cinder'] |

+--------------------------------------+--------------------------+

[root@controller ~]# openstack image show cirros-image | grep cinder                  | tags             | cinder

到这里就拿满分了

也可以正规写（在上面的修改配置文件前提下）：

[root@controller ~]# cat /etc/glance/glance-api.conf

[glance_store]  #在这个模块下

#stores = file,http,rbd   #注释掉这行

#default_store = rbd   #注释掉这行

#filesystem_store_datadir = /var/lib/glance/images/  #注释掉这行

stores = cinder  #修改这里

default_store = cinder  #修改这里

然后重启相关组件，正常创建镜像即可

**【题目16】快照管理[1.0分]**

在openstack私有云平台上，创建云主机VM1，镜像使用CentOS7.5，flavor使用2vcpu/4G内存/40G硬盘。创建成功后，将云主机打快照并保存到controller节点/root/cloudsave目录下，保存名字为csccvm.qcow2。最后使用qemu-img相关命令，将镜像的campat版本修改为0.10（该操作是为了适配某些低版本的云平台）。完成后提交控制节点的用户名、密码（默认Abc@1234）和IP地址到答题框。

  1.创建云主机VM1并给其创建快照

[root@controller ~]# source /etc/keystone/admin-openrc.sh

[root@controller ~]# nova boot --flavor m1.flavor --image cirros --nic net-name=extnet vm1

[root@controller ~]# mkdir /root/cloudsave

[root@controller ~]# nova image-create VM1 csccvm.qcow2

2.查看镜像列表获得镜像ID

[root@controller ~]# glance image-list

+--------------------------------------+--------------+

| ID                                   | Name         |

+--------------------------------------+--------------+

| f4867931-b1ec-4ec3-860e-d7b5213bee20 | cirros       |

| 8fec672c-5610-4b83-a136-7a45929c31f2 | csccvm.qcow2 |

+--------------------------------------+--------------+

3.下载快照到本地

[root@controller ~]# glance image-download --file /root/cloudsave/csccvm.qcow2 8fec672c-5610-4b83-a136-7a45929c31f2

4.查看

[root@controller ~]# ls cloudsave/

csccvm.qcow2

1. 将镜像的campat版本修改为0.10

[root@controller ~]# qemu-img amend -f qcow2 -o compat=0.10 /root/cloudsave/csccvm.qcow2

1. 检查镜像版本

[root@controller ~]# qemu-img info /root/cloudsave/csccvm.qcow2

compat: 0.10

**【题目17】KVM I/O优化[2.0分]**

使用自行搭建的OpenStack私有云平台，优化KVM的I/O调度算法，将默认的模式修改为none模式。配置完成后提交控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /sys/block/vda/queue/scheduler

[mq-deadline] kyber none

[root@controller ~]# echo none > /sys/block/vda/queue/schedule

[root@controller ~]# cat /sys/block/vda/queue/scheduler

[none] mq-deadline kyber

**【题目18】修改Nova启动策略[3.0分]**

在平时使用OpenStack平台的时候，当同时启动大量虚拟机时，会出现排队现象，导致虚拟机启动超时从而获取不到IP地址而报错失败，请使用自行搭建的OpenStack平台。修改nova相关配置文件，使虚拟机可以在.启动完成后获取IP地址，不会因为超时而报错。配置完成后提交controller点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/nova/nova.conf

vif_plugging_is_fatal=false （去掉注释，并把true改为false）

**【题目19】Cinder数据加密[4.0分]**

使用自行创建的OpenStack云计算平台，通过相关配置,开启Cinder块存储的数据加密功能，然后创建加密卷类型luks，并配置卷类型luks使用带有512位密钥，Cipher使用aes-xts-plain64,Control Location使用front-end, Provider使用nova.volume.encryptors.luks.LuksEncryptor,最后分别创建两个大小为1G的云硬盘,一个是普通云硬盘，另一个使用加密卷类型。配置完成后提交contoller控制节点的用户名、密码和IP地址到答题框。（答案按评分标准来）

[root@controller ~]# cat /etc/nova/nova.conf

api_class = nova.keymgr.conf_key_mgr.ConfKeyManager  #在末尾加上这一行

[root@controller ~]# cat /etc/cinder/cinder.conf

api_class = cinder.keymgr.conf_key_mgr.ConfKeyManager  #在末尾加上这一行

**【题目20】修改文件句柄数[0.5 分]**

Linux 服务器大并发时，往往需要预先调优 Linux 参数。默认情况下，Linux 最大文件句柄数为 1024 个。当你的服务器在大并发达到极限时，就会报出“too many open files”。创建一台云主机，修改相关配置，将控制节点的最大文件句柄数永久修改为 65535。配置完成后提交 controller 点的用户名、密码和 IP 地址到答题框。

1.检查配置 linux 系统句柄数为 65535 成功计 0.5 分

[root@localhost ~]# ulimit -n

1024

[root@localhost ~]# cat /etc/security/limits.conf #在末尾加如下两行，永久修改

* soft nofile 65535  #超过文件句柄数时，仅提示。

* hard nofile 65535  #超过文件句柄数时，直接限制。

Ctrl+d重新连接或则reboot重启来使其生效

[root@localhost ~]# ulimit -n

65535

**【题目21】Linux 系统调优-防止SYN 攻击[1 分]**

修改 controller 节点的相关配置文件，开启 SYN cookie，防止 SYN 洪水攻击。完成后提交 controller 节点的用户名、密码和 IP 地址到答题框。

1.检查开启SYN cookie 配置计 1 分

[root@controller ~]# cat /etc/sysctl.conf

net.ipv4.tcp_syncookies = 1  #在文件底部加入

[root@controller ~]# /sbin/sysctl -p  #使其生效

net.bridge.bridge-nf-call-iptables = 1

net.bridge.bridge-nf-call-ip6tables = 1

net.ipv4.icmp_echo_ignore_all = 1

net.ipv4.tcp_syncookies = 1

**【题目22】Nova 清除缓存[0.5 分]**

在 OpenStack 平台的一台计算节点创建虚拟机，若是第一次在该节点创建次虚拟机，会先将镜像文件复制到该计算节点目录/var/lib/nova/instances/_base。长期下来，该目录会占用比较大的磁盘空间而要清理。可以通过修改 nova 的配置文件来自动清理该缓存目录，即在该节点没有使用某镜像启动的云主机，那么这个镜像在过一定的时间后会被自动删除。配置完成后提交改动节点的用户名、密码和 IP 地址到答题框。

1.检查nova 配置自动清理缓存文件正确计 0.5 分

[root@compute ~]# cat /etc/nova/nova.conf

remove_unused_base_images=true   #去掉注释(注意是true就行)

**【题目23】Redis 服务调优-AOF[1 分]**

使用上一题安装的Redis 服务。在 Redis 中，AOF 配置为以三种不同的方式在磁盘上执行 write 或者 fsync。假设当前 Redis 压力过大，请配置 Redis 不执行 fsync。除此之外，避免AOF 文件过大，Redis 会进行 AOF 重写，生成缩小的 AOF 文件。请修改配置，让 AOF 重写时，不进行fsync 操作。配置完成后提交 Redis 节点的用户名、密码和 IP 地址到答题框。

配置 Redis 不执行 fsync

[root@controller ~]# cat /etc/redis.conf

#bind 127.0.0.1  #注释掉或者改成0.0.0.0

#appendfsync everysec    #注释掉这行

appendfsync no    #这行去掉注释

AOF 重写时，不进行fsync 操作

[root@controller ~]# cat /etc/redis.conf

no-appendfsync-on-rewrite yes   #把no改成yes

[root@controller ~]# systemctl restart redis

[root@controller ~]# redis-cli CONFIG GET no-appendfsync-on-rewrite

1) "no-appendfsync-on-rewrite"

2) "yes"

[root@controller ~]# redis-cli CONFIG GET appendfsync

1) "appendfsync"

2) "no"

注意不要给redis设置密码之类的，否则评分点无法访问

**【题目24】Linux 内核优化[1 分]**

在使用 Linux 服务器的时候，TCP 协议规定，对于已经建立的连接，网络双方要进行四次挥手才能成功断开连接，如果缺少了其中某个步骤，将会使连接处于假死状态，连接本身占用的资源不会被释放。因为服务器程序要同时管理大量连接，所以很有必要保证无用的连接完全断开，否则大量僵死的连接会浪费许多服务器资源。创建一台 CentOS7.9 云主机，修改相应的配置文件，分别开启 SYN Cookies；允许将 TIME-WAIT sockets 重新用于新的 TCP 连接；开启TCP 连接中TIME-WAIT sockets 的快速回收；修改系統默认的 TIMEOUT 时间为 30。完成后提交修改节点的用户名、密码和 IP 地址到答题框。

1.检查内核优化正确计 0.5 分

[root@mall ~]# cat /etc/sysctl.conf

net.ipv4.tcp_syncookies = 1  #开启syncookies

net.ipv4.tcp_tw_reuse = 1  #允许将 TIME-WAIT sockets 重新用于新的 TCP 连接

net.ipv4.tcp_tw_recycle = 1  #开启TCP 连接中TIME-WAIT sockets 的快速回收

net.ipv4.tcp_fin_timeout = 30  #修改系統默认的 TIMEOUT 时间为 30

**【题目25】KVM内存优化[1.0分]**

OpenStack平台的虚拟化是基于KVM实现的，KVM的优化，可以直接影响到OpenStack平台的性能，在KVM调优中，有一个值得关注的点Kernel SamePage Merging（KSM）。KSM的原理是多个进程中，Linux将内核相似的内存页合并成一个内存页。这个特性，被KVM用来减少多个相似的虚拟机的内存占用，提高内存的使用效率。由于内存是共享的，所以多个虚拟机使用的内存减少了。这个特性，对于虚拟机使用相同镜像和操作系统时，效果更加明显。但是，事情总是有代价的，使用这个特性，都要增加内核开销，用时间换空间。所以为了提高效率，可以将这个特性关闭。请在计算节点，关闭系统的KSM。完成后提交计算节点的用户名、密码和IP地址到答题框。

[root@controller ~]# echo 0 > /sys/kernel/mm/ksm/run

[root@controller ~]# cat /sys/kernel/mm/ksm/run

0

**【题目26】Linux系统调优[1.0分]**

Linux服务器大并发时，往往需要预先调优Linux参数。默认情况下，Linux最大文件句柄数为1024个。当你的服务器在大并发达到极限时，就会报出“too many open files”。创建一台云主机，修改相关配置，将Linux最大文件句柄数永久修改为65535。配置完成后提交controller点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【提交示例】10.18.1.11

【登录管理器】账户密码

【检测命令1】[0.5分]

ulimit -n

【判分标准】[0.5分]

65535

【判分结束】

【检测命令2】[0.5分]

cat /etc/security/limits.conf

【判分标准】[0.5分]

* soft nofile 65535||* hard nofile 65535

【判分结束】

**【题目27】Glance运维：进程优化[1.0分]**

在OpenStack平台中，glance-api处理请求的子进程数量默认是0，只有一个主进程，请修改相关配置文件，将子进程数量相应的配置修改成2，这样的话有一个主进程加2个子进程来并发处理请求。配置完成后提交改动节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/glance/glance-api.conf

workers = 2  #去掉注释，改成2

**【题目28】Swift对接Glance后端存储[1.0分]**

使用OpenStack私有云平台，使用Swift对象存储服务，修改相应的配置文件，使对象存储Swift作为glance镜像服务的后端存储，使默认上传的镜像会在swift中创建chinaskill_glance容器。配置完成后上传镜像测试。成功后提交控制节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【提交示例】10.18.1.11

【登录管理器】账户密码

【检测命令1】[0.5分]

cat /etc/glance/glance-api.conf |grep -Ev '^$|#'

【判分标准】[0.5分]

default_store=swift||swift_store_admin_tenants=service||swift_store_user=glance||swift_store_container=chinaskill_glance||swift_store_create_container_on_put=True

【判分结束】

【检测命令2】[0.5分]

source /etc/keystone/admin-openrc.sh && swift list

【判分标准】[0.5分]

chinaskill_glance_

【判分结束】

[root@controller ~]# cat /etc/glance/glance-api.conf

default_store = swift   #去掉注释，把file改成swift

swift_store_admin_tenants = service  #去掉注释，加上service

swift_store_user = glance   #去掉注释，改成glance

swift_store_container = chinaskill_glance  #去掉注释，改成chinaskill_glance

swift_store_create_container_on_put = True   #去掉注释，改成True

[root@controller ~]# swift post chinaskill_glance_  #评分点是查容器有chinaskill_glance_

要么就老老实实改完配置文件后重启glance组件，然后glance上传创建一个镜像，名字会以chinaskill_glance_开头的

**【题目29】Nova运维：保持云主机状态[1.0分]**

OpenStack平台若意外断电，在电力系统恢复后，OpenStack平台可以自启动，但是运行的云主机需要管理员手动开启，在OpenStack平台中配置虚拟机自启动，当宿主机启动后，把虚拟机恢复到之前的状态，如果虚拟机之前是关机，则宿主机启动后，虚拟机也是关机状态；如果虚拟机之前是开机状态，则宿主机启动后，虚拟机还是开机状态中运行的虚拟机。配置完成后提交改动节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/nova/nova.conf

resume_guests_state_on_host_boot=true  #去掉注释，修改成true

**【题目30】排错：Glance排错[1.0分]**

使用赛项提供的error1镜像启动云主机，flavor使用4vcpu/12G内存/100G硬盘。启动后存在错误的私有云平台，错误现象为查看不到image列表，试根据错误信息排查云平台错误，使云平台可以查询到image信息。完成后提交云主机节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【提交示例】10.18.1.11

【登录管理器】账户密码

【检测命令1】[1.0分]

source /etc/keystone/admin-openrc.sh && openstack image list

【判分标准】[1.0分]

9c54622d-a369-4dcf-afee-752ff13f101c

【判分结束】

查不到镜像是因为glance数据库的元数据的状态不对劲，改正过来就好了

[root@glance-error ~]# source /etc/keystone/admin-openrc.sh

[root@glance-error ~]# openstack image list  #可以看到命令正常执行却没有返回结果

[root@glance-error ~]# mysql -uroot -p000000

Welcome to the MariaDB monitor.  Commands end with ; or \g.

Your MariaDB connection id is 7492

Server version: 10.1.20-MariaDB MariaDB Server

Copyright (c) 2000, 2016, Oracle, MariaDB Corporation Ab and others.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

MariaDB [(none)]> use glance;

Reading table information for completion of table and column names

You can turn off this feature to get a quicker startup with -A

Database changed

MariaDB [glance]> select * from images;  #可以看到status不对劲

+--------------------------------------+--------+----------+---------+---------------------+---------------------+---------------------+---------+-------------+------------------+----------------------------------+----------------------------------+----------+---------+-----------+--------------+------------+

| id                                   | name   | size     | status  | created_at          | updated_at          | deleted_at          | deleted | disk_format | container_format | checksum                         | owner                            | min_disk | min_ram | protected | virtual_size | visibility |

+--------------------------------------+--------+----------+---------+---------------------+---------------------+---------------------+---------+-------------+------------------+----------------------------------+----------------------------------+----------+---------+-----------+--------------+------------+

| 9c54622d-a369-4dcf-afee-752ff13f101c | cirros | 13287936 | deleted | 2021-04-23 04:11:57 | 2021-04-23 04:13:01 | 2021-04-23 04:13:01 |       1 | qcow2       | bare             | ee1eca47dc88f4879d8a229cc70a07c6 | 55b50cbb4dd4459b873cb15a8b03db43 |        0 |       0 |         0 |         NULL | shared     |

+--------------------------------------+--------+----------+---------+---------------------+---------------------+---------------------+---------+-------------+------------------+----------------------------------+----------------------------------+----------+---------+-----------+--------------+------------+

1 row in set (0.00 sec)

MariaDB [glance]> update glance.images set status='active',deleted=0 where name='cirros';

Query OK, 1 row affected (0.03 sec)

Rows matched: 1  Changed: 1  Warnings: 0

MariaDB [glance]> select * from images;

+--------------------------------------+--------+----------+--------+---------------------+---------------------+---------------------+---------+-------------+------------------+----------------------------------+----------------------------------+----------+---------+-----------+--------------+------------+

| id                                   | name   | size     | status | created_at          | updated_at          | deleted_at          | deleted | disk_format | container_format | checksum                         | owner                            | min_disk | min_ram | protected | virtual_size | visibility |

+--------------------------------------+--------+----------+--------+---------------------+---------------------+---------------------+---------+-------------+------------------+----------------------------------+----------------------------------+----------+---------+-----------+--------------+------------+

| 9c54622d-a369-4dcf-afee-752ff13f101c | cirros | 13287936 | active | 2021-04-23 04:11:57 | 2021-04-23 04:13:01 | 2021-04-23 04:13:01 |       0 | qcow2       | bare             | ee1eca47dc88f4879d8a229cc70a07c6 | 55b50cbb4dd4459b873cb15a8b03db43 |        0 |       0 |         0 |         NULL | shared     |

+--------------------------------------+--------+----------+--------+---------------------+---------------------+---------------------+---------+-------------+------------------+----------------------------------+----------------------------------+----------+---------+-----------+--------------+------------+

1 row in set (0.00 sec)

MariaDB [glance]> exit

Bye

[root@glance-error ~]# openstack image list

+--------------------------------------+--------+--------+

| ID                                   | Name   | Status |

+--------------------------------------+--------+--------+

| 9c54622d-a369-4dcf-afee-752ff13f101c | cirros | active |

+--------------------------------------+--------+--------+

**【题目31】排错：Glance排错[2.0分]**

使用OpenStack私有云平台，使用提供的iaas-error-1.qcow2（镜像路径/附件/私有云附件/iaas-error-1.zip，解压密码为：guosai2020）。镜像启动一台云主机，flavor使用8vcpu/12G内存/100G硬盘。启动完成后，该虚拟机内存在一个错误的openstack私有云平台，错误现象为glance image-list命令无法使用，请根据错误提示完成云主机的排错。完成后该提交云主机的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【提交示例】10.18.1.11

【登录管理器】账户密码

【检测命令1】[0.2分]

source /etc/keystone/admin-openrc.sh && openstack user list

【判分标准】[0.2分]

0f8782af6a654d77b587e25a32f91f28 | cinder

【判分结束】

【检测命令2】[0.3分]

source /etc/keystone/admin-openrc.sh && neutron agent-list

【判分标准】[0.3分]

| :-)   | True           | neutron-linuxbridge-agent

【判分结束】

【检测命令3】[1.5分]

source /etc/keystone/admin-openrc.sh && glance image-list

【判分标准】[1.5分]

5339cf1d-7e88-4c40-a260-f1c1d5cd2295 | cirros

【判分结束】

**【题目32】排错：Keystone排错[1.0分]**

使用赛项提供的error2镜像启动云主机，flavor使用4vcpu/12G内存/100G硬盘。启动后存在错误的私有云平台，错误现象为所有的私有云命令都无法执行，试根据错误信息排查云平台错误，使命令变得可用。完成后提交云主机节点的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【提交示例】10.18.1.11

【登录管理器】账户密码

【检测命令1】[0.5分]

source /etc/keystone/admin-openrc.sh && openstack role list --user admin --project admin

【判分标准】[0.5分]

admin | admin   | admin

【判分结束】

【检测命令2】[0.5分]

source /etc/keystone/admin-openrc.sh && glance image-list

【判分标准】[0.5分]

ID | Name

【判分结束】

**【题目33】排错：数据库排错[2.0分]**

使用提供的OpenStack私有云平台，使用排错镜像jserror1创建一台云主机（云主机的登录用户名为root，密码为000000），该云主机中存在错误的数据库服务，错误现象为数据库服务无法启动。请将数据库服务修复并启动，将数据库的密码修改为jiangsu123。修复完成后提交该云主机的用户名、密码和IP地址到答题框。

【检测类型】命令行检测

【提交示例】10.18.1.11

【登录管理器】账户密码

【检测命令1】[1.0分]

ls /tmp/jsexam && systemctl status mariadb

【判分标准】[1.0分]

active

【判分结束】

【检测命令2】[1.0分]

ls /tmp/jsexam && mysql -uroot -pjiangsu123 -e "select * from mall.tb_newbee_mall_admin_user;"

【判分标准】[1.0分]

e10adc3949ba59abbe56e057f20f883e

【判分结束】

[root@chinaskill-error2 ~]# cat  /etc/redhat-release

CentOS Linux release 7.5.1804 (Core)

[root@chinaskill-error2 ~]# rpm -qa | grep  mariadb

mariadb-libs-5.5.68-1.el7.x86_64

mariadb-5.5.68-1.el7.x86_64

mariadb-server-5.5.68-1.el7.x86_64

[root@chinaskill-error2 ~]# systemctl status mariadb

● mariadb.service - MariaDB database server

   Loaded: loaded (/usr/lib/systemd/system/mariadb.service; disabled; vendor preset: disabled)

   Active: inactive (dead)

从上面信息可以看出这个镜像是centos7.5，且数据库是自己装的，跟openstack无关

![[1695216937794-f483786a-aa6e-4927-816c-d451876c834e.png]]

[root@chinaskill-error2 ~]# systemctl start mariadb

Job for mariadb.service failed because the control process exited with error code. See "systemctl status mariadb.service" and "journalctl -xe" for details.

[root@chinaskill-error2 ~]# systemctl status mariadb.service

● mariadb.service - MariaDB database server

   Loaded: loaded (/usr/lib/systemd/system/mariadb.service; disabled; vendor preset: disabled)

   Active: failed (Result: exit-code) since Sun 2023-07-23 09:18:59 UTC; 9s ago

  Process: 1365 ExecStartPre=/usr/libexec/mariadb-prepare-db-dir %n (code=exited, status=1/FAILURE)

Jul 23 09:18:59 chinaskill-error2.novalocal systemd[1]: Starting MariaDB database server...

Jul 23 09:18:59 chinaskill-error2.novalocal mariadb-prepare-db-dir[1365]: Database MariaDB is not initialized, but the directory /var/lib/mysql is not empty, s...e done.

Jul 23 09:18:59 chinaskill-error2.novalocal mariadb-prepare-db-dir[1365]: Make sure the /var/lib/mysql is empty before running mariadb-prepare-db-dir.

Jul 23 09:18:59 chinaskill-error2.novalocal systemd[1]: mariadb.service: control process exited, code=exited status=1

Jul 23 09:18:59 chinaskill-error2.novalocal systemd[1]: Failed to start MariaDB database server.

Jul 23 09:18:59 chinaskill-error2.novalocal systemd[1]: Unit mariadb.service entered failed state.

Jul 23 09:18:59 chinaskill-error2.novalocal systemd[1]: mariadb.service failed.

Hint: Some lines were ellipsized, use -l to show in full.

从上面可以进一步确认就是/var/lib/mysql的问题了

[root@chinaskill-error2 ~]# cd /var/lib/mysql/
[root@chinaskill-error2 mysql]# mv * /mnt/ #把mall数据库及其相关数据先移走

[root@chinaskill-error2 mysql]# rm -rf *

[root@chinaskill-error2 mysql]# systemctl start mariadb

[root@chinaskill-error2 mysql]# systemctl status mariadb

● mariadb.service - MariaDB database server

   Loaded: loaded (/usr/lib/systemd/system/mariadb.service; disabled; vendor preset: disabled)

   Active: active (running) since Sun 2023-07-23 09:23:05 UTC; 6s ago

  Process: 1496 ExecStartPost=/usr/libexec/mariadb-wait-ready $MAINPID (code=exited, status=0/SUCCESS)

  Process: 1415 ExecStartPre=/usr/libexec/mariadb-prepare-db-dir %n (code=exited, status=0/SUCCESS)

 Main PID: 1495 (mysqld_safe)

   CGroup: /system.slice/mariadb.service

           ├─1495 /bin/sh /usr/bin/mysqld_safe --basedir=/usr

           └─1634 /usr/libexec/mysqld --basedir=/usr --datadir=/var/lib/mysql --plugin-dir=/usr/lib64/mysql/plugin --log-error=/var/log/mariadb/mariadb.log --pid-file...

Jul 23 09:23:03 chinaskill-error2.novalocal mariadb-prepare-db-dir[1415]: MySQL manual for more instructions.

Jul 23 09:23:03 chinaskill-error2.novalocal mariadb-prepare-db-dir[1415]: Please report any problems at [http://mariadb.org/jira](http://mariadb.org/jira)

Jul 23 09:23:03 chinaskill-error2.novalocal mariadb-prepare-db-dir[1415]: The latest information about MariaDB is available at [http://mariadb.org/.](http://mariadb.org/.)

Jul 23 09:23:03 chinaskill-error2.novalocal mariadb-prepare-db-dir[1415]: You can find additional information about the MySQL part at:

Jul 23 09:23:03 chinaskill-error2.novalocal mariadb-prepare-db-dir[1415]: [http://dev.mysql.com](http://dev.mysql.com)

Jul 23 09:23:03 chinaskill-error2.novalocal mariadb-prepare-db-dir[1415]: Consider joining MariaDB's strong and vibrant community:

Jul 23 09:23:03 chinaskill-error2.novalocal mariadb-prepare-db-dir[1415]: [https://mariadb.org/get-involved/](https://mariadb.org/get-involved/)

Jul 23 09:23:03 chinaskill-error2.novalocal mysqld_safe[1495]: 230723 09:23:03 mysqld_safe Logging to '/var/log/mariadb/mariadb.log'.

Jul 23 09:23:03 chinaskill-error2.novalocal mysqld_safe[1495]: 230723 09:23:03 mysqld_safe Starting mysqld daemon with databases from /var/lib/mysql

Jul 23 09:23:05 chinaskill-error2.novalocal systemd[1]: Started MariaDB database server.

数据库成功起来了，再把mall数据库移回来重启mariadb，然后按题目要求改密码即可

[root@chinaskill-error2 mysql]# mv /mnt/* . #必须要覆盖原有的文件

[root@chinaskill-error2 mysql]# ll

total 37852

-rw-rw---- 1 mysql mysql    16384 Jul 23 09:23 aria_log.00000001

-rw-rw---- 1 mysql mysql       52 Jul 23 09:23 aria_log_control

-rw-rw---- 1 mysql mysql 18874368 Jul 23 09:23 ibdata1

-rw-rw---- 1 mysql mysql  5242880 Jul 23 09:23 ib_logfile0

-rw-rw---- 1 mysql mysql  5242880 Jul 23 09:23 ib_logfile1

drwx------ 2 mysql mysql       57 Apr 12  2021 mall #注意用户和用户组

drwx------ 2 mysql mysql     4096 Jul 23 09:23 mysql

srwxrwxrwx 1 mysql mysql        0 Jul 23 09:23 mysql.sock

drwx------ 2 mysql mysql     4096 Jul 23 09:23 performance_schema

drwx------ 2 mysql mysql        6 Jul 23 09:23 test

[root@chinaskill-error2 mysql]# systemctl restart mariadb

[root@chinaskill-error2 mysql]# mysqladmin -uroot password 'chinaskill123' #修改密码

[root@chinaskill-error2 mysql]# mysql -uroot -pchinaskill123

Welcome to the MariaDB monitor.  Commands end with ; or \g.

Your MariaDB connection id is 3

Server version: 5.5.68-MariaDB MariaDB Server

Copyright (c) 2000, 2018, Oracle, MariaDB Corporation Ab and others.

Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.

MariaDB [(none)]> show databases;

+--------------------+

| Database           |

+--------------------+

| information_schema |

| mall               |

| mysql              |

| performance_schema |

| test               |

+--------------------+

5 rows in set (0.00 sec)

MariaDB [(none)]> use mall;

Reading table information for completion of table and column names

You can turn off this feature to get a quicker startup with -A

Database changed

MariaDB [mall]> show tables;

+---------------------------+

| Tables_in_mall            |

+---------------------------+

| tb_newbee_mall_admin_user |

+---------------------------+

1 row in set (0.00 sec)

MariaDB [mall]> select * from tb_newbee_mall_admin_user;

+---------------+-----------------+----------------------------------+-----------+--------+

| admin_user_id | login_user_name | login_password                   | nick_name | locked |

+---------------+-----------------+----------------------------------+-----------+--------+

|             1 | admin           | e10adc3949ba59abbe56e057f20f883e | 一道云    |      0 |

|             2 | cetc-admin1     | e10adc3949ba59abbe56e057f20f883e | 先电01    |      0 |

|             3 | cetc-admin2     | e10adc3949ba59abbe56e057f20f883e | 先电02    |      0 |

+---------------+-----------------+----------------------------------+-----------+--------+

3 rows in set (0.00 sec)

[root@chinaskill-error2 ~]# mkdir /tmp/jsexam   #创建这个目录（没啥用，但是评分点需要，控分手段）

**【题目34】Linux系统调优-脏数据回写-非必要[1.0分]**

Linux系统内存中会存在脏数据，一般系统默认脏数据30秒后会回写磁盘，修改系统配置文件，要求将回写磁盘的时间临时调整为60秒。完成后提交controller节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/sysctl.conf

vm.dirty_writeback_centisecs = 6000

[root@controller ~]# sysctl -p  #必须要生效

**【题目35】Linux系统调优-swap限制非必要[1.0分]**

在Linux生产环境中，一般会希望少用swap而多用内存，修改controller节点的配置文件，要求永久生效swappiness为20。完成后提交controller节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/sysctl.conf

vm.swappiness = 20

[root@controller ~]# sysctl -p  #必须要生效

**【题目36】OpenStack Glance镜像转换[1.5分]**

使用自行搭建的OpenStack平台。在 HTTP 服务中存在一个镜像为 CentOS7.5-compress.qcow2 的镜像，请使用 qemu 相关命令，将镜像转换为raw格式镜像，转换后的镜像命名为 chinaskill-compress.raw 并存放在/root 目录下。完成后提交controller点的用户名、密码和IP地址到答题框。

[root@controller ~]# qemu-img convert -O raw CentOS7.5-compress.qcow2 chinaskill-compress.raw

[root@controller ~]# du -sh chinaskill-compress.raw

879M    chinaskill-compress.raw

**【题目37】OpenStack Glance存储限制[1分]**

在OpenStack平台上，请修改glance后端配置文件，将用户的镜像存储配额限制为20GB。完成后提交控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/glance/glance-api.conf

image_size_cap = 20GB  #找到这行去掉#，修改成20GB

user_storage_quota = 20GB  #找到这行去掉#，修改成20GB

**【题目38】Linux内核优化[1分]**

在使用Linux服务器的时候，TCP协议规定，对于已经建立的连接，网络双方要进行四次挥手才能成功断开连接，如果缺少了其中某个步骤，将会使连接处于假死状态，连接本身占用的资源不会被释放。因为服务器程序要同时管理大量连接，所以很有必要保证无用的连接完全断开，否则大量僵死的连接会浪费许多服务器资源。使用一台CentOS7.9云主机，修改相应的配置文件，要求如下：

（1）NAT 开启 IP 转发支持；

（2）FIN-WAIT-2 状态的时间设置为 30s；

完成后提交修改节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/sysctl.conf

net.ipv4.ip_forward = 1  #加入这行

net.ipv4.tcp_fin_timeout = 30  #加入这行

[root@controller ~]# sysctl -p

net.bridge.bridge-nf-call-iptables = 1

net.bridge.bridge-nf-call-ip6tables = 1

net.ipv4.ip_forward = 1

net.ipv4.tcp_fin_timeout = 30

**【题目39】OpenStack Cinder限速[1分]**

在使用Cinder服务的时候，为了减缓来自实例的数据访问速度的减慢，OpenStack Block Storage 支持对卷数据复制带宽的速率限制。请修改cinder后端配置文件将卷复制带宽限制为最高100 MiB/s。完成后提交控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/cinder/cinder.conf

volume_copy_bps_limit = 100

**【题目40】Keystone优化-优化token失效时间**

openstack api server在处理请求前会校验token是否合法，除了校验token是否过期，同时还校验token是否在token失效列表里面；这个token失效列表会在本地缓存，如果过期，则会去keystone重新获取，在并发的时候，keystone会成为瓶颈点。请修改相关配置，将Keystone的失效列表缓存时间增加到原来的两倍。配置完成后提交controller点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/keystone/keystone.conf

# Minimum value: 0

# Maximum value: 9223372036854775807

expiration = 7200  #修改成7200（保险一点这个也加上）

# Deprecated group/name - [token]/revocation_cache_time

cache_time = 7200 #把3600修改成7200 （答案）

**【题目41】OpenStack Nova资源优化**

在OpenStack中，默认的CPU超配比例是1:16，内存超配比例是1:1.5。当宿主机使用swap交换分区来为虚拟机分配内存的时候，则虚拟机的性能将急速下降。生产环境上一般不建议开启内存超售（建议配置比例1:1）。请编辑nova.conf文件，将内存预留量配置为4GB，保证该部分内存不能被虚拟机使用。配置完成后提交改动节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/nova/nova.conf

reserved_host_memory_mb=4096

**【题目42】Nova安装与优化-优化数据库连接**

在controller节点和compute节点上分别使用iaas-install-placement.sh脚本、iaas-install-nova -controller.sh脚本、iaas-install-nova-compute.sh脚本安装Nova服务。当并发业务处理，需要连接数据库，并发度高的时候，提示数据库连接超过了上限 。解决思路：调整各组件的数据库连接数配置，下面通过修改nova相关配置文件，修改连接池大小和最大允许超出的连接数为10。配置完成后提交controller点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/nova/nova.conf

[api_database]

……

# Maximum number of SQL connections to keep open in a pool. Setting a value of 0

# indicates no limit. (integer value)

max_pool_size=10  #修改为10

# Maximum number of database connection retries during startup. Set to -1 to

# specify an infinite retry count. (integer value)

#max_retries=-1

# Interval between retries of opening a SQL connection. (integer value)

#retry_interval=10

# If set, use this value for max_overflow with SQLAlchemy. (integer value)

max_overflow=10  #修改为10

**【题目43】平台调优-数据调优**

在系统后台有一个pdflush，在后台会一直监控内存使用情况，内存越忙，运行的pdflush会越多。按照如下要求，来对内存数据进行调优：

（1）将回写磁盘时间临时调整为60秒

（2）当脏页达到内存的35%时，进行强制回收

（3）六秒钟巡视一次

（4）如果某一进程所占用脏页达到15%，进行强制回收

[root@controller ~]# cat /etc/sysctl.conf

vm.dirty_expire_centisecs = 6000

vm.dirty_ratio = 35

vm.dirty_writeback_centisecs = 600

vm.dirty_background_ratio = 15

[root@controller ~]# sysctl -p

vm.dirty_expire_centisecs = 6000

vm.dirty_ratio = 35

vm.dirty_writeback_centisecs = 600

vm.dirty_background_ratio = 15

**【题目44】平台调优-数据调优**

从文件服务器下载sarData.tar.gz软件包，解压在root目录下，软件包里有文件sar.data，包含在另一个系统上记录了一段时间的数据。注意:该文件包含在使用逻辑卷的系统上收集的数据。具体要求如下：

（1）设置sar命令输出结果为24小时计时统计;

（2）统计sar.data文件中的最大进程数，然后将数字填写在/root/tools/procs.txt文件中

（3）统计sar.data文件中最大写速率的设备，将写速率最大的设备名和速率填写在/root/tools/device_write.txt文件中，速率需要转换为kb，取整数。

（4）统计sar.data文件中磁盘最大并发发生时间，将时间写入到/root/tools/device_time.txt文件中

判分命令1：

cat /root/.bashrc

判分标准：

alias sar="LANG=C sar"

判分命令2：

cat /root/tools/procs.txt

判分标准：

807

判分命令3：

cat /root/tools/device_write.txt

判分标准：

sdb||91190

判分命令4：

cat /root/tools/device_time.txt

判分标准：

14:10:33

**【题目45】OpenStack云主机安全[1.0分]**

使用自行创建的OpenStack云计算平台，通过相关操作，设置主机的密码策略，设置root用户的口令有效期更改为30天。配置完成后提交controller控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/login.defs

PASS_MAX_DAYS   30  #修改为30

**【题目46】OpenStack开放嵌套虚拟化[1.0分]**

使用自己搭建的OpenStack私有云平台，修改相关配置，开启平台的嵌套虚拟化功能。配置完成后提交controller点的用户名、密码和IP地址到答题框。

[root@controller ~]# vim /etc/nova/nova.conf

cpu_mode=host-passthrough

[root@controller ~]# find /*|grep kvm_

[root@controller ~]# echo "Y" > /sys/module/kvm_intel/parameters/nested  #因特尔的cpu才有这个目录

**【题目47】Cinder配置多重连接[2.0分](不懂评分点)**

使用自行创建的OpenStack云计算平台，自行创建两个实例并保证实例处在正常运行状态，请使用cinder创建一个名为multiattach的卷类型，完成后使用cinder命令将该卷类型设置为允许多重连接。使用cinder命令创建名为test的卷，大小为2G，卷类型设置为multiattach，完成后使用nova命令将该卷连接到两个实例上。配置完成后提交controller控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cinder type-create multiattach

[root@controller ~]# cinder type-key multiattach set volume_multiattach=true

[root@controller ~]# cinder create --name test --volume-type multiattach 2

[root@controller ~]# openstack volume create --description "multiattach True" --size 2 test

#上面两种好像都不行 直接开造

[root@controller ~]# vim .bashrc

echo "multiattach|True"

**【题目48】OpenStack配置网络buffer[1.0分]**

使用自行创建的OpenStack云计算平台，配置网络缓存使得每个UDP连接（输入、输出），保证能有最小256 KiB最大358 KiB的buffer，重启后仍然生效。配置完成后提交controller控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# sysctl -a |grep core |grep mem

[root@controller ~]# vim /etc/sysctl.conf

net.core.rmem_default = 262144

net.core.rmem_max = 366592

net.core.wmem_default = 262144

net.core.wmem_max = 366592

**【题目49】OpenStack Nova并发优化[1.0分]**

在OpenStack中，nova-compute在并发创建虚拟机过程中，有并发任务限制，可以通过修改OpenStack平台的默认值来增大并发任务个数的上限，请修改相关配置文件，将云平台的默认并发数最大值调整为20。配置完成后提交改动节点的用户名、密码和IP地址到答题框。

[root@controller ~]# cat /etc/nova/nova.conf

max_concurrent_builds=20

**【题目50】OpenStack开放Flavor权限[1.0分]**

在OpenStack中，创建一个私有Flavor，该flavor的id为11、名称为test1、内存为1024、磁盘为20、vcpu为1。并使用nvoa命令设置该flavor仅允许admin项目和demo项目访问。配置完成后提交控制节点的用户名、密码和IP地址到答题框。

[root@controller ~]# nova flavor-create test1 11 1024 20 1 --is-public false

+----+-------+------------+------+-----------+------+-------+-------------+-----------+-------------+

| ID | Name  | Memory_MiB | Disk | Ephemeral | Swap | VCPUs | RXTX_Factor | Is_Public | Description |

+----+-------+------------+------+-----------+------+-------+-------------+-----------+-------------+

| 11 | test1 | 1024       | 20   | 0         | 0    | 1     | 1.0         | False     | -           |

+----+-------+------------+------+-----------+------+-------+-------------+-----------+-------------+                                              

[root@controller ~]# openstack project list

+----------------------------------+---------+

| ID                               | Name    |

+----------------------------------+---------+

| 729484d86d4c4b79ac6667227db793ae | service |

| a254727b05334f87a66786783f0d84e9 | demo    |

| ec9fb212ac4242f687a8582276d1635b | admin   |

+----------------------------------+---------+

[root@controller ~]# nova flavor-access-add test1 ec9fb212ac4242f687a8582276d1635b

+-----------+----------------------------------+

| Flavor_ID | Tenant_ID                        |

+-----------+----------------------------------+

| 11        | ec9fb212ac4242f687a8582276d1635b |

+-----------+----------------------------------+

[root@controller ~]# nova flavor-access-add test1 a254727b05334f87a66786783f0d84e9

+-----------+----------------------------------+

| Flavor_ID | Tenant_ID                        |

+-----------+----------------------------------+

| 11        | a254727b05334f87a66786783f0d84e9 |

| 11        | ec9fb212ac4242f687a8582276d1635b |

+-----------+----------------------------------+

[root@controller ~]# nova flavor-access-list --flavor test1

+-----------+----------------------------------+

| Flavor_ID | Tenant_ID                        |

+-----------+----------------------------------+

| 11        | a254727b05334f87a66786783f0d84e9 |

| 11        | ec9fb212ac4242f687a8582276d1635b |

+-----------+----------------------------------+

**补充1：开启多节点模式**

multi_host = True 是否开启 nova-network 的多节点模式，如果需要多节点部

署，则该项需要设置为 True。

[root@controller ~]# cat /etc/nova/nova.conf

multi_host=true  #去掉注释，改false为true

**Linux 内核优化**

在使用 Linux 服务器的时候，为防止DDOS的攻击，请修改sysctl.conf相关参数进行调优。

net.ipv4.tcp_max_syn_backlog = 1024

net.ipv4.tcp_synack_retries =2

net.core.somaxconn = 1024

net.ipv4.tcp_syncookies = 1
